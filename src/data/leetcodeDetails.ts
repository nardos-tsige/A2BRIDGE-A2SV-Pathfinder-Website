export const LEETCODE_DETAILS: Record<string, { description: string; explanation: string; implementation: string }> = {
  "Two Sum": {
    description: "Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.",
    explanation: "Use a hash map to store the value and its index as you iterate through the array. For each element, check if the complement (target - current value) exists in the hash map. This gives an O(n) time complexity.",
    implementation: "class Solution:\\n    def twoSum(self, nums: List[int], target: int) -> List[int]:\\n        seen = {}\\n        for i, num in enumerate(nums):\\n            if target - num in seen:\\n                return [seen[target - num], i]\\n            seen[num] = i\\n        return []"
  },
  "Best Time to Buy and Sell Stock": {
    description: "You are given an array prices where prices[i] is the price of a given stock on the ith day. Return the maximum profit you can achieve from this transaction.",
    explanation: "Keep track of the minimum price seen so far and the maximum profit. Iterate through the prices, updating the minimum price and calculating the potential profit at each step. O(n) time complexity.",
    implementation: "class Solution:\\n    def maxProfit(self, prices: List[int]) -> int:\\n        min_price = float('inf')\\n        max_profit = 0\\n        for price in prices:\\n            min_price = min(min_price, price)\\n            max_profit = max(max_profit, price - min_price)\\n        return max_profit"
  },
  "Contains Duplicate": {
    description: "Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.",
    explanation: "Use a hash set to keep track of seen elements. If an element is already in the set, return True. If the loop finishes, return False. O(n) time and O(n) space.",
    implementation: "class Solution:\\n    def containsDuplicate(self, nums: List[int]) -> bool:\\n        return len(set(nums)) != len(nums)"
  },
  "Product of Array Except Self": {
    description: "Given an integer array nums, return an array answer such that answer[i] is equal to the product of all the elements of nums except nums[i].",
    explanation: "Calculate the prefix products and suffix products for each element. You can optimize space by storing the prefix products in the result array and computing the suffix products on the fly. O(n) time.",
    implementation: "class Solution:\\n    def productExceptSelf(self, nums: List[int]) -> List[int]:\\n        res = [1] * len(nums)\\n        prefix = 1\\n        for i in range(len(nums)):\\n            res[i] = prefix\\n            prefix *= nums[i]\\n        postfix = 1\\n        for i in range(len(nums) - 1, -1, -1):\\n            res[i] *= postfix\\n            postfix *= nums[i]\\n        return res"
  },
  "Maximum Subarray": {
    description: "Given an integer array nums, find the contiguous subarray (containing at least one number) which has the largest sum and return its sum.",
    explanation: "Use Kadane's Algorithm. Keep a running sum of the current subarray. If the running sum becomes negative, reset it to 0. Update the maximum sum seen so far at each step.",
    implementation: "class Solution:\\n    def maxSubArray(self, nums: List[int]) -> int:\\n        max_sum = nums[0]\\n        current_sum = 0\\n        for num in nums:\\n            if current_sum < 0:\\n                current_sum = 0\\n            current_sum += num\\n            max_sum = max(max_sum, current_sum)\\n        return max_sum"
  },
  "Maximum Product Subarray": {
    description: "Given an integer array nums, find a contiguous non-empty subarray within the array that has the largest product, and return the product.",
    explanation: "Keep track of both the maximum and minimum product ending at the current position, because a negative number multiplied by a minimum (negative) product can become the new maximum.",
    implementation: "class Solution:\\n    def maxProduct(self, nums: List[int]) -> int:\\n        res = max(nums)\\n        curMin, curMax = 1, 1\\n        for n in nums:\\n            if n == 0:\\n                curMin, curMax = 1, 1\\n                continue\\n            tmp = curMax * n\\n            curMax = max(n * curMax, n * curMin, n)\\n            curMin = min(tmp, n * curMin, n)\\n            res = max(res, curMax)\\n        return res"
  },
  "Find Minimum in Rotated Sorted Array": {
    description: "Suppose an array of length n sorted in ascending order is rotated between 1 and n times. Given the sorted rotated array nums of unique elements, return the minimum element of this array.",
    explanation: "Use binary search. If the middle element is greater than the rightmost element, the minimum is to the right. Otherwise, it is to the left (including mid).",
    implementation: "class Solution:\\n    def findMin(self, nums: List[int]) -> int:\\n        l, r = 0, len(nums) - 1\\n        while l < r:\\n            m = (l + r) // 2\\n            if nums[m] > nums[r]:\\n                l = m + 1\\n            else:\\n                r = m\\n        return nums[l]"
  },
  "Search in Rotated Sorted Array": {
    description: "Given the array nums after the possible rotation and an integer target, return the index of target if it is in nums, or -1 if it is not in nums.",
    explanation: "Use binary search. Determine which half of the array is sorted. Check if the target lies within the sorted half. If it does, narrow the search to that half; otherwise, search the other half.",
    implementation: "class Solution:\\n    def search(self, nums: List[int], target: int) -> int:\\n        l, r = 0, len(nums) - 1\\n        while l <= r:\\n            mid = (l + r) // 2\\n            if nums[mid] == target: return mid\\n            if nums[l] <= nums[mid]:\\n                if nums[l] <= target < nums[mid]: r = mid - 1\\n                else: l = mid + 1\\n            else:\\n                if nums[mid] < target <= nums[r]: l = mid + 1\\n                else: r = mid - 1\\n        return -1"
  },
  "3Sum": {
    description: "Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i != j, i != k, and j != k, and nums[i] + nums[j] + nums[k] == 0.",
    explanation: "Sort the array. Iterate through the array, and for each element, use two pointers (left and right) to find two other elements that sum to the negative of the current element. Skip duplicates to avoid duplicate triplets.",
    implementation: "class Solution:\\n    def threeSum(self, nums: List[int]) -> List[List[int]]:\\n        res = []\\n        nums.sort()\\n        for i, a in enumerate(nums):\\n            if i > 0 and a == nums[i - 1]: continue\\n            l, r = i + 1, len(nums) - 1\\n            while l < r:\\n                threeSum = a + nums[l] + nums[r]\\n                if threeSum > 0: r -= 1\\n                elif threeSum < 0: l += 1\\n                else:\\n                    res.append([a, nums[l], nums[r]])\\n                    l += 1\\n                    while nums[l] == nums[l - 1] and l < r: l += 1\\n        return res"
  },
  "Container With Most Water": {
    description: "You are given an integer array height of length n. There are n vertical lines drawn such that the two endpoints of the ith line are (i, 0) and (i, height[i]). Find two lines that together with the x-axis form a container, such that the container contains the most water.",
    explanation: "Use two pointers, one at the beginning and one at the end. The area is limited by the shorter line. Move the pointer pointing to the shorter line inward to potentially find a taller line and a larger area.",
    implementation: "class Solution:\\n    def maxArea(self, height: List[int]) -> int:\\n        res = 0\\n        l, r = 0, len(height) - 1\\n        while l < r:\\n            area = (r - l) * min(height[l], height[r])\\n            res = max(res, area)\\n            if height[l] < height[r]: l += 1\\n            else: r -= 1\\n        return res"
  },
  "Sum of Two Integers": {
    description: "Given two integers a and b, return the sum of the two integers without using the operators + and -.",
    explanation: "Use bit manipulation. The XOR operator (^) performs addition without carry. The AND operator (&) followed by a left shift (<< 1) calculates the carry. Repeat until there is no carry.",
    implementation: "class Solution:\\n    def getSum(self, a: int, b: int) -> int:\\n        mask = 0xFFFFFFFF\\n        while b != 0:\\n            a, b = (a ^ b) & mask, ((a & b) << 1) & mask\\n        return a if a <= 0x7FFFFFFF else ~(a ^ mask)"
  },
  "Number of 1 Bits": {
    description: "Write a function that takes an unsigned integer and returns the number of '1' bits it has (also known as the Hamming weight).",
    explanation: "Use the bitwise AND operation n & (n - 1) to clear the lowest set bit. Count how many times you can do this until the number becomes 0.",
    implementation: "class Solution:\\n    def hammingWeight(self, n: int) -> int:\\n        res = 0\\n        while n:\\n            n &= (n - 1)\\n            res += 1\\n        return res"
  },
  "Counting Bits": {
    description: "Given an integer n, return an array ans of length n + 1 such that for each i (0 <= i <= n), ans[i] is the number of 1's in the binary representation of i.",
    explanation: "Use dynamic programming. The number of 1s in binary representation of i is equal to the number of 1s in i // 2 plus 1 if i is odd.",
    implementation: "class Solution:\\n    def countBits(self, n: int) -> List[int]:\\n        dp = [0] * (n + 1)\\n        offset = 1\\n        for i in range(1, n + 1):\\n            if offset * 2 == i: offset = i\\n            dp[i] = 1 + dp[i - offset]\\n        return dp"
  },
  "Missing Number": {
    description: "Given an array nums containing n distinct numbers in the range [0, n], return the only number in the range that is missing from the array.",
    explanation: "Calculate the expected sum of numbers from 0 to n using the formula n * (n + 1) / 2. Subtract the actual sum of the array from the expected sum to find the missing number.",
    implementation: "class Solution:\\n    def missingNumber(self, nums: List[int]) -> int:\\n        n = len(nums)\\n        return n * (n + 1) // 2 - sum(nums)"
  },
  "Reverse Bits": {
    description: "Reverse bits of a given 32 bits unsigned integer.",
    explanation: "Iterate 32 times. In each iteration, extract the rightmost bit of n, shift the result to the left, and add the extracted bit. Then shift n to the right.",
    implementation: "class Solution:\\n    def reverseBits(self, n: int) -> int:\\n        res = 0\\n        for i in range(32):\\n            bit = (n >> i) & 1\\n            res = res | (bit << (31 - i))\\n        return res"
  },
  "Climbing Stairs": {
    description: "You are climbing a staircase. It takes n steps to reach the top. Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?",
    explanation: "This is a Fibonacci sequence problem. The number of ways to reach step n is the sum of ways to reach step n-1 and step n-2. Use dynamic programming to store previous results.",
    implementation: "class Solution:\\n    def climbStairs(self, n: int) -> int:\\n        one, two = 1, 1\\n        for i in range(n - 1):\\n            temp = one\\n            one = one + two\\n            two = temp\\n        return one"
  },
  "Coin Change": {
    description: "You are given an integer array coins representing coins of different denominations and an integer amount representing a total amount of money. Return the fewest number of coins that you need to make up that amount.",
    explanation: "Use dynamic programming. Create an array dp where dp[i] is the minimum coins needed for amount i. Initialize with infinity. For each amount, try all coins and update dp[i] = min(dp[i], 1 + dp[i - coin]).",
    implementation: "class Solution:\\n    def coinChange(self, coins: List[int], amount: int) -> int:\\n        dp = [float('inf')] * (amount + 1)\\n        dp[0] = 0\\n        for a in range(1, amount + 1):\\n            for c in coins:\\n                if a - c >= 0:\\n                    dp[a] = min(dp[a], 1 + dp[a - c])\\n        return dp[amount] if dp[amount] != float('inf') else -1"
  },
  "Longest Increasing Subsequence": {
    description: "Given an integer array nums, return the length of the longest strictly increasing subsequence.",
    explanation: "Use dynamic programming. dp[i] represents the length of the LIS ending at index i. For each element, check all previous elements. If a previous element is smaller, update dp[i] = max(dp[i], 1 + dp[prev]).",
    implementation: "class Solution:\\n    def lengthOfLIS(self, nums: List[int]) -> int:\\n        LIS = [1] * len(nums)\\n        for i in range(len(nums) - 1, -1, -1):\\n            for j in range(i + 1, len(nums)):\\n                if nums[i] < nums[j]:\\n                    LIS[i] = max(LIS[i], 1 + LIS[j])\\n        return max(LIS)"
  },
  "Longest Common Subsequence": {
    description: "Given two strings text1 and text2, return the length of their longest common subsequence. If there is no common subsequence, return 0.",
    explanation: "Use a 2D DP table. If characters match, dp[i][j] = 1 + dp[i+1][j+1]. If they don't match, take the max of skipping a character from either string: max(dp[i+1][j], dp[i][j+1]).",
    implementation: "class Solution:\\n    def longestCommonSubsequence(self, text1: str, text2: str) -> int:\\n        dp = [[0 for j in range(len(text2) + 1)] for i in range(len(text1) + 1)]\\n        for i in range(len(text1) - 1, -1, -1):\\n            for j in range(len(text2) - 1, -1, -1):\\n                if text1[i] == text2[j]:\\n                    dp[i][j] = 1 + dp[i + 1][j + 1]\\n                else:\\n                    dp[i][j] = max(dp[i][j + 1], dp[i + 1][j])\\n        return dp[0][0]"
  },
  "Word Break": {
    description: "Given a string s and a dictionary of strings wordDict, return true if s can be segmented into a space-separated sequence of one or more dictionary words.",
    explanation: "Use dynamic programming. dp[i] is True if the substring up to index i can be segmented. For each index, check if any word in the dictionary matches the substring starting at i.",
    implementation: "class Solution:\\n    def wordBreak(self, s: str, wordDict: List[str]) -> bool:\\n        dp = [False] * (len(s) + 1)\\n        dp[len(s)] = True\\n        for i in range(len(s) - 1, -1, -1):\\n            for w in wordDict:\\n                if (i + len(w)) <= len(s) and s[i : i + len(w)] == w:\\n                    dp[i] = dp[i + len(w)]\\n                if dp[i]:\\n                    break\\n        return dp[0]"
  },
  "Combination Sum": {
    description: "Given an array of distinct integers candidates and a target integer target, return a list of all unique combinations of candidates where the chosen numbers sum to target.",
    explanation: "Use backtracking. At each step, you can either include the current candidate (and stay on the same index since reuse is allowed) or skip to the next candidate. Base cases are when target == 0 (success) or target < 0 (fail).",
    implementation: "class Solution:\\n    def combinationSum(self, candidates: List[int], target: int) -> List[List[int]]:\\n        res = []\\n        def dfs(i, cur, total):\\n            if total == target:\\n                res.append(cur.copy())\\n                return\\n            if i >= len(candidates) or total > target:\\n                return\\n            cur.append(candidates[i])\\n            dfs(i, cur, total + candidates[i])\\n            cur.pop()\\n            dfs(i + 1, cur, total)\\n        dfs(0, [], 0)\\n        return res"
  },
  "House Robber": {
    description: "You are a professional robber planning to rob houses along a street. Each house has a certain amount of money stashed, the only constraint stopping you from robbing each of them is that adjacent houses have security systems connected and it will automatically contact the police if two adjacent houses were broken into on the same night.",
    explanation: "Use dynamic programming. For each house, you can either rob it (adding its value to the max loot from two houses ago) or skip it (keeping the max loot from the previous house).",
    implementation: "class Solution:\\n    def rob(self, nums: List[int]) -> int:\\n        rob1, rob2 = 0, 0\\n        for n in nums:\\n            temp = max(n + rob1, rob2)\\n            rob1 = rob2\\n            rob2 = temp\\n        return rob2"
  },
  "House Robber II": {
    description: "You are a professional robber planning to rob houses along a street. Each house has a certain amount of money stashed. All houses at this place are arranged in a circle. That means the first house is the neighbor of the last one.",
    explanation: "Since the houses are in a circle, you cannot rob both the first and last house. Run the standard House Robber algorithm twice: once excluding the first house, and once excluding the last house. Return the maximum.",
    implementation: "class Solution:\\n    def rob(self, nums: List[int]) -> int:\\n        return max(nums[0], self.helper(nums[1:]), self.helper(nums[:-1]))\\n    def helper(self, nums):\\n        rob1, rob2 = 0, 0\\n        for n in nums:\\n            newRob = max(rob1 + n, rob2)\\n            rob1 = rob2\\n            rob2 = newRob\\n        return rob2"
  },
  "Decode Ways": {
    description: "A message containing letters from A-Z can be encoded into numbers using the following mapping: 'A' -> \"1\", 'B' -> \"2\", ... 'Z' -> \"26\". Given a string s containing only digits, return the number of ways to decode it.",
    explanation: "Use dynamic programming. dp[i] represents the number of ways to decode the string up to index i. Check if the single digit is valid (1-9) and if the two digits are valid (10-26).",
    implementation: "class Solution:\\n    def numDecodings(self, s: str) -> int:\\n        dp = {len(s): 1}\\n        for i in range(len(s) - 1, -1, -1):\\n            if s[i] == '0':\\n                dp[i] = 0\\n            else:\\n                dp[i] = dp[i + 1]\\n            if i + 1 < len(s) and (s[i] == '1' or s[i] == '2' and s[i + 1] in '0123456'):\\n                dp[i] += dp[i + 2]\\n        return dp[0]"
  },
  "Unique Paths": {
    description: "There is a robot on an m x n grid. The robot is initially located at the top-left corner (i.e., grid[0][0]). The robot tries to move to the bottom-right corner (i.e., grid[m - 1][n - 1]). The robot can only move either down or right at any point in time.",
    explanation: "Use dynamic programming. The number of ways to reach a cell is the sum of the ways to reach the cell above it and the cell to its left. Initialize the top row and left column to 1.",
    implementation: "class Solution:\\n    def uniquePaths(self, m: int, n: int) -> int:\\n        row = [1] * n\\n        for i in range(m - 1):\\n            newRow = [1] * n\\n            for j in range(n - 2, -1, -1):\\n                newRow[j] = newRow[j + 1] + row[j]\\n            row = newRow\\n        return row[0]"
  },
  "Jump Game": {
    description: "You are given an integer array nums. You are initially positioned at the array's first index, and each element in the array represents your maximum jump length at that position. Return true if you can reach the last index, or false otherwise.",
    explanation: "Use a greedy approach. Keep track of the 'goal' index, initially the last index. Iterate backwards. If the current index plus its jump length can reach or exceed the goal, update the goal to the current index. If goal reaches 0, return True.",
    implementation: "class Solution:\\n    def canJump(self, nums: List[int]) -> bool:\\n        goal = len(nums) - 1\\n        for i in range(len(nums) - 1, -1, -1):\\n            if i + nums[i] >= goal:\\n                goal = i\\n        return True if goal == 0 else False"
  },
  "Clone Graph": {
    description: "Given a reference of a node in a connected undirected graph. Return a deep copy (clone) of the graph.",
    explanation: "Use Depth First Search (DFS) and a hash map to keep track of already cloned nodes. For each node, create a copy, add it to the map, and recursively clone its neighbors.",
    implementation: "class Solution:\\n    def cloneGraph(self, node: 'Node') -> 'Node':\\n        oldToNew = {}\\n        def dfs(node):\\n            if node in oldToNew: return oldToNew[node]\\n            copy = Node(node.val)\\n            oldToNew[node] = copy\\n            for nei in node.neighbors:\\n                copy.neighbors.append(dfs(nei))\\n            return copy\\n        return dfs(node) if node else None"
  },
  "Course Schedule": {
    description: "There are a total of numCourses courses you have to take, labeled from 0 to numCourses - 1. You are given an array prerequisites where prerequisites[i] = [ai, bi] indicates that you must take course bi first if you want to take course ai. Return true if you can finish all courses. Otherwise, return false.",
    explanation: "This is a cycle detection problem in a directed graph. Use DFS to detect cycles. Keep track of nodes currently in the recursion stack (visiting). If you encounter a node already in the visiting set, a cycle exists.",
    implementation: "class Solution:\\n    def canFinish(self, numCourses: int, prerequisites: List[List[int]]) -> bool:\\n        preMap = {i: [] for i in range(numCourses)}\\n        for crs, pre in prerequisites:\\n            preMap[crs].append(pre)\\n        visitSet = set()\\n        def dfs(crs):\\n            if crs in visitSet: return False\\n            if preMap[crs] == []: return True\\n            visitSet.add(crs)\\n            for pre in preMap[crs]:\\n                if not dfs(pre): return False\\n            visitSet.remove(crs)\\n            preMap[crs] = []\\n            return True\\n        for crs in range(numCourses):\\n            if not dfs(crs): return False\\n        return True"
  },
  "Pacific Atlantic Water Flow": {
    description: "There is an m x n rectangular island that borders both the Pacific Ocean and Atlantic Ocean. The Pacific Ocean touches the island's left and top edges, and the Atlantic Ocean touches the island's right and bottom edges. Return a list of grid coordinates result where result[i] = [ri, ci] denotes that rain water can flow from cell (ri, ci) to both the Pacific and Atlantic oceans.",
    explanation: "Start DFS/BFS from the ocean borders and work inwards, finding all cells that can reach the Pacific and all cells that can reach the Atlantic. The intersection of these two sets is the answer.",
    implementation: "class Solution:\\n    def pacificAtlantic(self, heights: List[List[int]]) -> List[List[int]]:\\n        ROWS, COLS = len(heights), len(heights[0])\\n        pac, atl = set(), set()\\n        def dfs(r, c, visit, prevHeight):\\n            if ((r, c) in visit or r < 0 or c < 0 or r == ROWS or c == COLS or heights[r][c] < prevHeight):\\n                return\\n            visit.add((r, c))\\n            dfs(r + 1, c, visit, heights[r][c])\\n            dfs(r - 1, c, visit, heights[r][c])\\n            dfs(r, c + 1, visit, heights[r][c])\\n            dfs(r, c - 1, visit, heights[r][c])\\n        for c in range(COLS):\\n            dfs(0, c, pac, heights[0][c])\\n            dfs(ROWS - 1, c, atl, heights[ROWS - 1][c])\\n        for r in range(ROWS):\\n            dfs(r, 0, pac, heights[r][0])\\n            dfs(r, COLS - 1, atl, heights[r][COLS - 1])\\n        res = []\\n        for r in range(ROWS):\\n            for c in range(COLS):\\n                if (r, c) in pac and (r, c) in atl:\\n                    res.append([r, c])\\n        return res"
  },
  "Number of Islands": {
    description: "Given an m x n 2D binary grid grid which represents a map of '1's (land) and '0's (water), return the number of islands. An island is surrounded by water and is formed by connecting adjacent lands horizontally or vertically.",
    explanation: "Iterate through the grid. When you find a '1' (land), increment the island count and use DFS or BFS to mark all connected land as visited (e.g., by changing '1' to '0').",
    implementation: "class Solution:\\n    def numIslands(self, grid: List[List[str]]) -> int:\\n        if not grid: return 0\\n        rows, cols = len(grid), len(grid[0])\\n        islands = 0\\n        def dfs(r, c):\\n            if r < 0 or c < 0 or r >= rows or c >= cols or grid[r][c] == '0':\\n                return\\n            grid[r][c] = '0'\\n            dfs(r + 1, c)\\n            dfs(r - 1, c)\\n            dfs(r, c + 1)\\n            dfs(r, c - 1)\\n        for r in range(rows):\\n            for c in range(cols):\\n                if grid[r][c] == '1':\\n                    islands += 1\\n                    dfs(r, c)\\n        return islands"
  },
  "Longest Consecutive Sequence": {
    description: "Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence. You must write an algorithm that runs in O(n) time.",
    explanation: "Convert the array to a hash set for O(1) lookups. For each number, check if it's the start of a sequence (i.e., num - 1 is not in the set). If it is, count the length of the sequence by repeatedly checking for num + 1.",
    implementation: "class Solution:\\n    def longestConsecutive(self, nums: List[int]) -> int:\\n        numSet = set(nums)\\n        longest = 0\\n        for n in nums:\\n            if (n - 1) not in numSet:\\n                length = 1\\n                while (n + length) in numSet:\\n                    length += 1\\n                longest = max(longest, length)\\n        return longest"
  },
  "Alien Dictionary": {
    description: "There is a new alien language that uses the English alphabet. However, the order among the letters is unknown to you. You are given a list of strings words from the alien language's dictionary, where the strings in words are sorted lexicographically by the rules of this new language. Return a string of the unique letters in the new alien language sorted in lexicographically increasing order by the new language's rules.",
    explanation: "Build a directed graph where edges represent character precedence. Perform a topological sort using DFS. If a cycle is detected, return an empty string.",
    implementation: "class Solution:\\n    def alienOrder(self, words: List[str]) -> str:\\n        adj = {c: set() for w in words for c in w}\\n        for i in range(len(words) - 1):\\n            w1, w2 = words[i], words[i + 1]\\n            minLen = min(len(w1), len(w2))\\n            if len(w1) > len(w2) and w1[:minLen] == w2[:minLen]: return ''\\n            for j in range(minLen):\\n                if w1[j] != w2[j]:\\n                    adj[w1[j]].add(w2[j])\\n                    break\\n        visit = {}\\n        res = []\\n        def dfs(c):\\n            if c in visit: return visit[c]\\n            visit[c] = True\\n            for nei in adj[c]:\\n                if dfs(nei): return True\\n            visit[c] = False\\n            res.append(c)\\n        for c in adj:\\n            if dfs(c): return ''\\n        res.reverse()\\n        return ''.join(res)"
  },
  "Graph Valid Tree": {
    description: "You have a graph of n nodes labeled from 0 to n - 1. You are given an integer n and a list of edges where edges[i] = [ai, bi] indicates that there is an undirected edge between nodes ai and bi in the graph. Return true if the edges of the given graph make up a valid tree, and false otherwise.",
    explanation: "A valid tree must have exactly n - 1 edges and all nodes must be connected. Use DFS or Union-Find to check for cycles and connectivity.",
    implementation: "class Solution:\\n    def validTree(self, n: int, edges: List[List[int]]) -> bool:\\n        if not n: return True\\n        adj = {i: [] for i in range(n)}\\n        for n1, n2 in edges:\\n            adj[n1].append(n2)\\n            adj[n2].append(n1)\\n        visit = set()\\n        def dfs(i, prev):\\n            if i in visit: return False\\n            visit.add(i)\\n            for j in adj[i]:\\n                if j == prev: continue\\n                if not dfs(j, i): return False\\n            return True\\n        return dfs(0, -1) and n == len(visit)"
  },
  "Number of Connected Components in an Undirected Graph": {
    description: "You have a graph of n nodes. You are given an integer n and an array edges where edges[i] = [ai, bi] indicates that there is an edge between ai and bi in the graph. Return the number of connected components in the graph.",
    explanation: "Use Union-Find or DFS/BFS. For Union-Find, initialize n components. For each edge, union the two nodes. If they belong to different components, decrement the component count.",
    implementation: "class Solution:\\n    def countComponents(self, n: int, edges: List[List[int]]) -> int:\\n        par = [i for i in range(n)]\\n        rank = [1] * n\\n        def find(n1):\\n            res = n1\\n            while res != par[res]:\\n                par[res] = par[par[res]]\\n                res = par[res]\\n            return res\\n        def union(n1, n2):\\n            p1, p2 = find(n1), find(n2)\\n            if p1 == p2: return 0\\n            if rank[p2] > rank[p1]:\\n                par[p1] = p2\\n                rank[p2] += rank[p1]\\n            else:\\n                par[p2] = p1\\n                rank[p1] += rank[p2]\\n            return 1\\n        res = n\\n        for n1, n2 in edges:\\n            res -= union(n1, n2)\\n        return res"
  },
  "Insert Interval": {
    description: "You are given an array of non-overlapping intervals intervals where intervals[i] = [starti, endi] represent the start and the end of the ith interval and intervals is sorted in ascending order by starti. You are also given an interval newInterval = [start, end] that represents the start and end of another interval. Insert newInterval into intervals such that intervals is still sorted in ascending order by starti and intervals still does not have any overlapping intervals (merge overlapping intervals if necessary).",
    explanation: "Iterate through the intervals. If the current interval ends before the new interval starts, add it to the result. If it starts after the new interval ends, add the new interval and the rest. Otherwise, merge them by updating the new interval's start and end.",
    implementation: "class Solution:\\n    def insert(self, intervals: List[List[int]], newInterval: List[int]) -> List[List[int]]:\\n        res = []\\n        for i in range(len(intervals)):\\n            if newInterval[1] < intervals[i][0]:\\n                res.append(newInterval)\\n                return res + intervals[i:]\\n            elif newInterval[0] > intervals[i][1]:\\n                res.append(intervals[i])\\n            else:\\n                newInterval = [min(newInterval[0], intervals[i][0]), max(newInterval[1], intervals[i][1])]\\n        res.append(newInterval)\\n        return res"
  },
  "Merge Intervals": {
    description: "Given an array of intervals where intervals[i] = [starti, endi], merge all overlapping intervals, and return an array of the non-overlapping intervals that cover all the intervals in the input.",
    explanation: "Sort the intervals by their start time. Iterate through them, and if the current interval overlaps with the last merged interval, update the end time of the last merged interval. Otherwise, add the current interval to the result.",
    implementation: "class Solution:\\n    def merge(self, intervals: List[List[int]]) -> List[List[int]]:\\n        intervals.sort(key=lambda i: i[0])\\n        output = [intervals[0]]\\n        for start, end in intervals[1:]:\\n            lastEnd = output[-1][1]\\n            if start <= lastEnd:\\n                output[-1][1] = max(lastEnd, end)\\n            else:\\n                output.append([start, end])\\n        return output"
  },
  "Non-overlapping Intervals": {
    description: "Given an array of intervals intervals where intervals[i] = [starti, endi], return the minimum number of intervals you need to remove to make the rest of the intervals non-overlapping.",
    explanation: "Sort the intervals by their end time. Iterate through them, keeping track of the end time of the last non-overlapping interval. If the current interval starts before the last end time, it overlaps, so increment the removal count.",
    implementation: "class Solution:\\n    def eraseOverlapIntervals(self, intervals: List[List[int]]) -> int:\\n        intervals.sort(key=lambda i: i[1])\\n        res = 0\\n        prevEnd = intervals[0][1]\\n        for start, end in intervals[1:]:\\n            if start >= prevEnd:\\n                prevEnd = end\\n            else:\\n                res += 1\\n        return res"
  },
  "Meeting Rooms": {
    description: "Given an array of meeting time intervals where intervals[i] = [starti, endi], determine if a person could attend all meetings.",
    explanation: "Sort the intervals by start time. Check if any interval starts before the previous one ends. If so, return False.",
    implementation: "class Solution:\\n    def canAttendMeetings(self, intervals: List[List[int]]) -> bool:\\n        intervals.sort(key=lambda i: i[0])\\n        for i in range(1, len(intervals)):\\n            if intervals[i][0] < intervals[i - 1][1]:\\n                return False\\n        return True"
  },
  "Meeting Rooms II": {
    description: "Given an array of meeting time intervals intervals where intervals[i] = [starti, endi], return the minimum number of conference rooms required.",
    explanation: "Separate start and end times into two sorted arrays. Use two pointers to iterate through them. If a meeting starts before the earliest ending meeting ends, you need a new room. Otherwise, a room frees up.",
    implementation: "class Solution:\\n    def minMeetingRooms(self, intervals: List[List[int]]) -> int:\\n        start = sorted([i[0] for i in intervals])\\n        end = sorted([i[1] for i in intervals])\\n        res, count = 0, 0\\n        s, e = 0, 0\\n        while s < len(intervals):\\n            if start[s] < end[e]:\\n                s += 1\\n                count += 1\\n            else:\\n                e += 1\\n                count -= 1\\n            res = max(res, count)\\n        return res"
  },
  "Reverse Linked List": {
    description: "Given the head of a singly linked list, reverse the list, and return the reversed list.",
    explanation: "Use two pointers, prev (initially None) and curr (initially head). Iterate through the list, saving the next node, reversing the current node's pointer to prev, and then advancing prev and curr.",
    implementation: "class Solution:\\n    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:\\n        prev, curr = None, head\\n        while curr:\\n            nxt = curr.next\\n            curr.next = prev\\n            prev = curr\\n            curr = nxt\\n        return prev"
  },
  "Linked List Cycle": {
    description: "Given head, the head of a linked list, determine if the linked list has a cycle in it.",
    explanation: "Use Floyd's Tortoise and Hare algorithm. Have a slow pointer move one step and a fast pointer move two steps. If they ever meet, there is a cycle.",
    implementation: "class Solution:\\n    def hasCycle(self, head: Optional[ListNode]) -> bool:\\n        slow, fast = head, head\\n        while fast and fast.next:\\n            slow = slow.next\\n            fast = fast.next.next\\n            if slow == fast:\\n                return True\\n        return False"
  },
  "Merge Two Sorted Lists": {
    description: "You are given the heads of two sorted linked lists list1 and list2. Merge the two lists in a one sorted list. The list should be made by splicing together the nodes of the first two lists.",
    explanation: "Use a dummy node to simplify edge cases. Compare the heads of the two lists, append the smaller one to the merged list, and advance the corresponding pointer.",
    implementation: "class Solution:\\n    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:\\n        dummy = ListNode()\\n        tail = dummy\\n        while list1 and list2:\\n            if list1.val < list2.val:\\n                tail.next = list1\\n                list1 = list1.next\\n            else:\\n                tail.next = list2\\n                list2 = list2.next\\n            tail = tail.next\\n        if list1:\\n            tail.next = list1\\n        elif list2:\\n            tail.next = list2\\n        return dummy.next"
  },
  "Merge k Sorted Lists": {
    description: "You are given an array of k linked-lists lists, each linked-list is sorted in ascending order. Merge all the linked-lists into one sorted linked-list and return it.",
    explanation: "Use a min-heap (priority queue) or divide and conquer. For divide and conquer, pair up lists and merge them using the 'Merge Two Sorted Lists' algorithm until only one list remains.",
    implementation: "class Solution:\\n    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:\\n        if not lists or len(lists) == 0: return None\\n        while len(lists) > 1:\\n            mergedLists = []\\n            for i in range(0, len(lists), 2):\\n                l1 = lists[i]\\n                l2 = lists[i + 1] if (i + 1) < len(lists) else None\\n                mergedLists.append(self.mergeList(l1, l2))\\n            lists = mergedLists\\n        return lists[0]\\n    def mergeList(self, l1, l2):\\n        dummy = ListNode()\\n        tail = dummy\\n        while l1 and l2:\\n            if l1.val < l2.val:\\n                tail.next = l1\\n                l1 = l1.next\\n            else:\\n                tail.next = l2\\n                l2 = l2.next\\n            tail = tail.next\\n        if l1: tail.next = l1\\n        if l2: tail.next = l2\\n        return dummy.next"
  },
  "Remove Nth Node From End of List": {
    description: "Given the head of a linked list, remove the nth node from the end of the list and return its head.",
    explanation: "Use a dummy node and two pointers, left and right. Move the right pointer n steps ahead. Then move both pointers until the right pointer reaches the end. The left pointer will be just before the node to remove.",
    implementation: "class Solution:\\n    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:\\n        dummy = ListNode(0, head)\\n        left = dummy\\n        right = head\\n        while n > 0:\\n            right = right.next\\n            n -= 1\\n        while right:\\n            left = left.next\\n            right = right.next\\n        left.next = left.next.next\\n        return dummy.next"
  },
  "Reorder List": {
    description: "You are given the head of a singly linked-list. The list can be represented as: L0 → L1 → … → Ln - 1 → Ln. Reorder the list to be on the following form: L0 → Ln → L1 → Ln - 1 → L2 → Ln - 2 → …",
    explanation: "Find the middle of the list using slow and fast pointers. Reverse the second half of the list. Then merge the two halves by alternating nodes.",
    implementation: "class Solution:\\n    def reorderList(self, head: Optional[ListNode]) -> None:\\n        slow, fast = head, head.next\\n        while fast and fast.next:\\n            slow = slow.next\\n            fast = fast.next.next\\n        second = slow.next\\n        prev = slow.next = None\\n        while second:\\n            tmp = second.next\\n            second.next = prev\\n            prev = second\\n            second = tmp\\n        first, second = head, prev\\n        while second:\\n            tmp1, tmp2 = first.next, second.next\\n            first.next = second\\n            second.next = tmp1\\n            first, second = tmp1, tmp2"
  },
  "Set Matrix Zeroes": {
    description: "Given an m x n integer matrix matrix, if an element is 0, set its entire row and column to 0's. You must do it in place.",
    explanation: "Use the first row and first column as markers to indicate if a row or column should be zeroed. Use a separate variable for the first row/column to avoid overlapping.",
    implementation: "class Solution:\\n    def setZeroes(self, matrix: List[List[int]]) -> None:\\n        ROWS, COLS = len(matrix), len(matrix[0])\\n        rowZero = False\\n        for r in range(ROWS):\\n            for c in range(COLS):\\n                if matrix[r][c] == 0:\\n                    matrix[0][c] = 0\\n                    if r > 0: matrix[r][0] = 0\\n                    else: rowZero = True\\n        for r in range(1, ROWS):\\n            for c in range(1, COLS):\\n                if matrix[0][c] == 0 or matrix[r][0] == 0:\\n                    matrix[r][c] = 0\\n        if matrix[0][0] == 0:\\n            for r in range(ROWS):\\n                matrix[r][0] = 0\\n        if rowZero:\\n            for c in range(COLS):\\n                matrix[0][c] = 0"
  },
  "Spiral Matrix": {
    description: "Given an m x n matrix, return all elements of the matrix in spiral order.",
    explanation: "Keep track of the boundaries: left, right, top, bottom. Iterate in a spiral pattern (right, down, left, up), shrinking the boundaries after each direction is completed.",
    implementation: "class Solution:\\n    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:\\n        res = []\\n        left, right = 0, len(matrix[0])\\n        top, bottom = 0, len(matrix)\\n        while left < right and top < bottom:\\n            for i in range(left, right):\\n                res.append(matrix[top][i])\\n            top += 1\\n            for i in range(top, bottom):\\n                res.append(matrix[i][right - 1])\\n            right -= 1\\n            if not (left < right and top < bottom): break\\n            for i in range(right - 1, left - 1, -1):\\n                res.append(matrix[bottom - 1][i])\\n            bottom -= 1\\n            for i in range(bottom - 1, top - 1, -1):\\n                res.append(matrix[i][left])\\n            left += 1\\n        return res"
  },
  "Rotate Image": {
    description: "You are given an n x n 2D matrix representing an image, rotate the image by 90 degrees (clockwise). You have to rotate the image in-place, which means you have to modify the input 2D matrix directly.",
    explanation: "Rotate the matrix layer by layer. For each layer, move elements in a 4-way swap. Alternatively, transpose the matrix (swap rows and columns) and then reverse each row.",
    implementation: "class Solution:\\n    def rotate(self, matrix: List[List[int]]) -> None:\\n        l, r = 0, len(matrix) - 1\\n        while l < r:\\n            for i in range(r - l):\\n                top, bottom = l, r\\n                topLeft = matrix[top][l + i]\\n                matrix[top][l + i] = matrix[bottom - i][l]\\n                matrix[bottom - i][l] = matrix[bottom][r - i]\\n                matrix[bottom][r - i] = matrix[top + i][r]\\n                matrix[top + i][r] = topLeft\\n            r -= 1\\n            l += 1"
  },
  "Word Search": {
    description: "Given an m x n grid of characters board and a string word, return true if word exists in the grid. The word can be constructed from letters of sequentially adjacent cells, where adjacent cells are horizontally or vertically neighboring.",
    explanation: "Use backtracking (DFS). For each cell, if it matches the first letter of the word, start a DFS. Keep track of visited cells in the current path to avoid reusing them.",
    implementation: "class Solution:\\n    def exist(self, board: List[List[str]], word: str) -> bool:\\n        ROWS, COLS = len(board), len(board[0])\\n        path = set()\\n        def dfs(r, c, i):\\n            if i == len(word): return True\\n            if (r < 0 or c < 0 or r >= ROWS or c >= COLS or word[i] != board[r][c] or (r, c) in path):\\n                return False\\n            path.add((r, c))\\n            res = (dfs(r + 1, c, i + 1) or dfs(r - 1, c, i + 1) or dfs(r, c + 1, i + 1) or dfs(r, c - 1, i + 1))\\n            path.remove((r, c))\\n            return res\\n        for r in range(ROWS):\\n            for c in range(COLS):\\n                if dfs(r, c, 0): return True\\n        return False"
  },
  "Longest Substring Without Repeating Characters": {
    description: "Given a string s, find the length of the longest substring without repeating characters.",
    explanation: "Use a sliding window with a hash set. Keep adding characters to the set. If a duplicate is found, remove characters from the left until the duplicate is gone. Update the max length at each step.",
    implementation: "class Solution:\\n    def lengthOfLongestSubstring(self, s: str) -> int:\\n        charSet = set()\\n        l = 0\\n        res = 0\\n        for r in range(len(s)):\\n            while s[r] in charSet:\\n                charSet.remove(s[l])\\n                l += 1\\n            charSet.add(s[r])\\n            res = max(res, r - l + 1)\\n        return res"
  },
  "Valid Anagram": {
    description: "Given two strings s and t, return true if t is an anagram of s, and false otherwise.",
    explanation: "Count the frequency of each character in both strings. If the frequencies match for all characters, they are anagrams. You can use a hash map or an array of size 26 for lowercase English letters.",
    implementation: "class Solution:\\n    def isAnagram(self, s: str, t: str) -> bool:\\n        if len(s) != len(t): return False\\n        countS, countT = {}, {}\\n        for i in range(len(s)):\\n            countS[s[i]] = 1 + countS.get(s[i], 0)\\n            countT[t[i]] = 1 + countT.get(t[i], 0)\\n        return countS == countT"
  },
  "Valid Parentheses": {
    description: "Given a string s containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.",
    explanation: "Use a stack. Iterate through the string. If it's an opening bracket, push it onto the stack. If it's a closing bracket, check if the stack is not empty and the top of the stack is the corresponding opening bracket. If so, pop it. Otherwise, return False.",
    implementation: "class Solution:\\n    def isValid(self, s: str) -> bool:\\n        stack = []\\n        closeToOpen = {')': '(', ']': '[', '}': '{'}\\n        for c in s:\\n            if c in closeToOpen:\\n                if stack and stack[-1] == closeToOpen[c]:\\n                    stack.pop()\\n                else:\\n                    return False\\n            else:\\n                stack.append(c)\\n        return True if not stack else False"
  },
  "Valid Palindrome": {
    description: "A phrase is a palindrome if, after converting all uppercase letters into lowercase letters and removing all non-alphanumeric characters, it reads the same forward and backward. Alphanumeric characters include letters and numbers. Given a string s, return true if it is a palindrome, or false otherwise.",
    explanation: "Use two pointers, one at the beginning and one at the end. Move them towards the center, skipping non-alphanumeric characters. Compare the characters at the pointers (ignoring case). If they don't match, return False.",
    implementation: "class Solution:\\n    def isPalindrome(self, s: str) -> bool:\\n        l, r = 0, len(s) - 1\\n        while l < r:\\n            while l < r and not self.alphaNum(s[l]): l += 1\\n            while r > l and not self.alphaNum(s[r]): r -= 1\\n            if s[l].lower() != s[r].lower(): return False\\n            l, r = l + 1, r - 1\\n        return True\\n    def alphaNum(self, c):\\n        return (ord('A') <= ord(c) <= ord('Z') or ord('a') <= ord(c) <= ord('z') or ord('0') <= ord(c) <= ord('9'))"
  },
  "Maximum Depth of Binary Tree": {
    description: "Given the root of a binary tree, return its maximum depth. A binary tree's maximum depth is the number of nodes along the longest path from the root node down to the farthest leaf node.",
    explanation: "Use recursion (DFS). The maximum depth of a tree is 1 plus the maximum depth of its left and right subtrees. Base case is an empty tree (depth 0).",
    implementation: "class Solution:\\n    def maxDepth(self, root: Optional[TreeNode]) -> int:\\n        if not root: return 0\\n        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))"
  },
  "Longest Repeating Character Replacement": {
    description: "You are given a string s and an integer k. You can choose any character of the string and change it to any other uppercase English character. You can perform this operation at most k times. Return the length of the longest substring containing the same letter you can get after performing the above operations.",
    explanation: "Use a sliding window. Keep a count of character frequencies in the current window. The window is valid if the window length minus the count of the most frequent character is <= k.",
    implementation: "class Solution:\\n    def characterReplacement(self, s: str, k: int) -> int:\\n        count = {}\\n        res = 0\\n        l = 0\\n        maxf = 0\\n        for r in range(len(s)):\\n            count[s[r]] = 1 + count.get(s[r], 0)\\n            maxf = max(maxf, count[s[r]])\\n            while (r - l + 1) - maxf > k:\\n                count[s[l]] -= 1\\n                l += 1\\n            res = max(res, r - l + 1)\\n        return res"
  },
  "Minimum Window Substring": {
    description: "Given two strings s and t of lengths m and n respectively, return the minimum window substring of s such that every character in t (including duplicates) is included in the window. If there is no such substring, return the empty string \"\".",
    explanation: "Use a sliding window and two hash maps (or one). Expand the right pointer to include characters until the window contains all characters of t. Then shrink the left pointer to minimize the window while still containing all characters of t.",
    implementation: "class Solution:\\n    def minWindow(self, s: str, t: str) -> str:\\n        if t == '': return ''\\n        countT, window = {}, {}\\n        for c in t:\\n            countT[c] = 1 + countT.get(c, 0)\\n        have, need = 0, len(countT)\\n        res, resLen = [-1, -1], float('inf')\\n        l = 0\\n        for r in range(len(s)):\\n            c = s[r]\\n            window[c] = 1 + window.get(c, 0)\\n            if c in countT and window[c] == countT[c]:\\n                have += 1\\n            while have == need:\\n                if (r - l + 1) < resLen:\\n                    res = [l, r]\\n                    resLen = (r - l + 1)\\n                window[s[l]] -= 1\\n                if s[l] in countT and window[s[l]] < countT[s[l]]:\\n                    have -= 1\\n                l += 1\\n        l, r = res\\n        return s[l:r+1] if resLen != float('inf') else ''"
  },
  "Group Anagrams": {
    description: "Given an array of strings strs, group the anagrams together. You can return the answer in any order.",
    explanation: "Use a hash map where the key is the character count (as a tuple) and the value is a list of anagrams. Iterate through the strings, compute their character counts, and group them.",
    implementation: "from collections import defaultdict\\nclass Solution:\\n    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:\\n        res = defaultdict(list)\\n        for s in strs:\\n            count = [0] * 26\\n            for c in s:\\n                count[ord(c) - ord('a')] += 1\\n            res[tuple(count)].append(s)\\n        return list(res.values())"
  },
  "Longest Palindromic Substring": {
    description: "Given a string s, return the longest palindromic substring in s.",
    explanation: "Expand around center. For each character (and each pair of adjacent characters), expand outwards as long as the characters match. Keep track of the longest palindrome found.",
    implementation: "class Solution:\\n    def longestPalindrome(self, s: str) -> str:\\n        res = ''\\n        resLen = 0\\n        for i in range(len(s)):\\n            l, r = i, i\\n            while l >= 0 and r < len(s) and s[l] == s[r]:\\n                if (r - l + 1) > resLen:\\n                    res = s[l:r+1]\\n                    resLen = r - l + 1\\n                l -= 1\\n                r += 1\\n            l, r = i, i + 1\\n            while l >= 0 and r < len(s) and s[l] == s[r]:\\n                if (r - l + 1) > resLen:\\n                    res = s[l:r+1]\\n                    resLen = r - l + 1\\n                l -= 1\\n                r += 1\\n        return res"
  },
  "Palindromic Substrings": {
    description: "Given a string s, return the number of palindromic substrings in it. A string is a palindrome when it reads the same backward as forward. A substring is a contiguous sequence of characters within the string.",
    explanation: "Similar to Longest Palindromic Substring, expand around center. For each character and each pair of adjacent characters, expand outwards and count every valid palindrome found.",
    implementation: "class Solution:\\n    def countSubstrings(self, s: str) -> int:\\n        res = 0\\n        for i in range(len(s)):\\n            l = r = i\\n            while l >= 0 and r < len(s) and s[l] == s[r]:\\n                res += 1\\n                l -= 1\\n                r += 1\\n            l = i\\n            r = i + 1\\n            while l >= 0 and r < len(s) and s[l] == s[r]:\\n                res += 1\\n                l -= 1\\n                r += 1\\n        return res"
  },
  "Encode and Decode Strings": {
    description: "Design an algorithm to encode a list of strings to a string. The encoded string is then sent over the network and is decoded back to the original list of strings.",
    explanation: "To encode, prefix each string with its length followed by a delimiter (e.g., '5#hello'). To decode, read the length, skip the delimiter, and extract the string.",
    implementation: "class Solution:\\n    def encode(self, strs: List[str]) -> str:\\n        res = ''\\n        for s in strs:\\n            res += str(len(s)) + '#' + s\\n        return res\\n\\n    def decode(self, s: str) -> List[str]:\\n        res, i = [], 0\\n        while i < len(s):\\n            j = i\\n            while s[j] != '#':\\n                j += 1\\n            length = int(s[i:j])\\n            res.append(s[j + 1 : j + 1 + length])\\n            i = j + 1 + length\\n        return res"
  },
  "Same Tree": {
    description: "Given the roots of two binary trees p and q, write a function to check if they are the same or not. Two binary trees are considered the same if they are structurally identical, and the nodes have the same value.",
    explanation: "Use recursion (DFS). If both nodes are null, return True. If one is null or values don't match, return False. Recursively check left and right subtrees.",
    implementation: "class Solution:\\n    def isSameTree(self, p: Optional[TreeNode], q: Optional[TreeNode]) -> bool:\\n        if not p and not q: return True\\n        if not p or not q or p.val != q.val: return False\\n        return self.isSameTree(p.left, q.left) and self.isSameTree(p.right, q.right)"
  },
  "Invert Binary Tree": {
    description: "Given the root of a binary tree, invert the tree, and return its root.",
    explanation: "Use recursion (DFS). Swap the left and right children of the current node, then recursively invert the left and right subtrees.",
    implementation: "class Solution:\\n    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:\\n        if not root: return None\\n        tmp = root.left\\n        root.left = root.right\\n        root.right = tmp\\n        self.invertTree(root.left)\\n        self.invertTree(root.right)\\n        return root"
  },
  "Binary Tree Maximum Path Sum": {
    description: "A path in a binary tree is a sequence of nodes where each pair of adjacent nodes in the sequence has an edge connecting them. A node can only appear in the sequence at most once. Note that the path does not need to pass through the root. The path sum of a path is the sum of the node's values in the path. Given the root of a binary tree, return the maximum path sum of any non-empty path.",
    explanation: "Use DFS. For each node, compute the max path sum of its left and right subtrees (ignoring negative sums). Update the global max with the path passing through the current node. Return the max path extending from the current node to its parent.",
    implementation: "class Solution:\\n    def maxPathSum(self, root: Optional[TreeNode]) -> int:\\n        res = [root.val]\\n        def dfs(root):\\n            if not root: return 0\\n            leftMax = max(dfs(root.left), 0)\\n            rightMax = max(dfs(root.right), 0)\\n            res[0] = max(res[0], root.val + leftMax + rightMax)\\n            return root.val + max(leftMax, rightMax)\\n        dfs(root)\\n        return res[0]"
  },
  "Binary Tree Level Order Traversal": {
    description: "Given the root of a binary tree, return the level order traversal of its nodes' values. (i.e., from left to right, level by level).",
    explanation: "Use BFS with a queue. For each level, iterate through the current nodes in the queue, add their values to a level list, and enqueue their children.",
    implementation: "import collections\\nclass Solution:\\n    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:\\n        res = []\\n        q = collections.deque()\\n        if root: q.append(root)\\n        while q:\\n            val = []\\n            for i in range(len(q)):\\n                node = q.popleft()\\n                val.append(node.val)\\n                if node.left: q.append(node.left)\\n                if node.right: q.append(node.right)\\n            res.append(val)\\n        return res"
  },
  "Subtree of Another Tree": {
    description: "Given the roots of two binary trees root and subRoot, return true if there is a subtree of root with the same structure and node values of subRoot and false otherwise.",
    explanation: "Use DFS. For each node in the main tree, check if the subtree starting at that node is identical to the target tree (using the Same Tree logic).",
    implementation: "class Solution:\\n    def isSubtree(self, root: Optional[TreeNode], subRoot: Optional[TreeNode]) -> bool:\\n        if not subRoot: return True\\n        if not root: return False\\n        if self.sameTree(root, subRoot): return True\\n        return self.isSubtree(root.left, subRoot) or self.isSubtree(root.right, subRoot)\\n    def sameTree(self, s, t):\\n        if not s and not t: return True\\n        if s and t and s.val == t.val:\\n            return self.sameTree(s.left, t.left) and self.sameTree(s.right, t.right)\\n        return False"
  },
  "Construct Binary Tree from Preorder and Inorder Traversal": {
    description: "Given two integer arrays preorder and inorder where preorder is the preorder traversal of a binary tree and inorder is the inorder traversal of the same tree, construct and return the binary tree.",
    explanation: "The first element in preorder is the root. Find this root in inorder to split the tree into left and right subtrees. Recursively build the left and right subtrees.",
    implementation: "class Solution:\\n    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:\\n        if not preorder or not inorder: return None\\n        root = TreeNode(preorder[0])\\n        mid = inorder.index(preorder[0])\\n        root.left = self.buildTree(preorder[1:mid + 1], inorder[:mid])\\n        root.right = self.buildTree(preorder[mid + 1:], inorder[mid + 1:])\\n        return root"
  },
  "Validate Binary Search Tree": {
    description: "Given the root of a binary tree, determine if it is a valid binary search tree (BST).",
    explanation: "Use DFS. Pass down the valid range (min_val, max_val) for each node. A node's value must be strictly between its min_val and max_val.",
    implementation: "class Solution:\\n    def isValidBST(self, root: Optional[TreeNode]) -> bool:\\n        def valid(node, left, right):\\n            if not node: return True\\n            if not (node.val < right and node.val > left): return False\\n            return valid(node.left, left, node.val) and valid(node.right, node.val, right)\\n        return valid(root, float('-inf'), float('inf'))"
  },
  "Kth Smallest Element in a BST": {
    description: "Given the root of a binary search tree, and an integer k, return the kth smallest value (1-indexed) of all the values of the nodes in the tree.",
    explanation: "Use inorder traversal (iterative with a stack or recursive). Inorder traversal of a BST visits nodes in ascending order. Keep track of the number of visited nodes and return the kth one.",
    implementation: "class Solution:\\n    def kthSmallest(self, root: Optional[TreeNode], k: int) -> int:\\n        n = 0\\n        stack = []\\n        curr = root\\n        while curr or stack:\\n            while curr:\\n                stack.append(curr)\\n                curr = curr.left\\n            curr = stack.pop()\\n            n += 1\\n            if n == k: return curr.val\\n            curr = curr.right"
  },
  "Lowest Common Ancestor of a BST": {
    description: "Given a binary search tree (BST), find the lowest common ancestor (LCA) node of two given nodes in the BST.",
    explanation: "Use the BST property. If both p and q are greater than the root, the LCA is in the right subtree. If both are less, it's in the left subtree. Otherwise, the current root is the LCA.",
    implementation: "class Solution:\\n    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':\\n        curr = root\\n        while curr:\\n            if p.val > curr.val and q.val > curr.val:\\n                curr = curr.right\\n            elif p.val < curr.val and q.val < curr.val:\\n                curr = curr.left\\n            else:\\n                return curr"
  },
  "Implement Trie (Prefix Tree)": {
    description: "A trie (pronounced as \"try\") or prefix tree is a tree data structure used to efficiently store and retrieve keys in a dataset of strings. There are various applications of this very efficient data structure, such as autocomplete and spellchecker. Implement the Trie class.",
    explanation: "Use a tree structure where each node represents a character and contains a dictionary of its children and a boolean flag indicating the end of a word.",
    implementation: "class TrieNode:\\n    def __init__(self):\\n        self.children = {}\\n        self.endOfWord = False\\nclass Trie:\\n    def __init__(self):\\n        self.root = TrieNode()\\n    def insert(self, word: str) -> None:\\n        cur = self.root\\n        for c in word:\\n            if c not in cur.children:\\n                cur.children[c] = TrieNode()\\n            cur = cur.children[c]\\n        cur.endOfWord = True\\n    def search(self, word: str) -> bool:\\n        cur = self.root\\n        for c in word:\\n            if c not in cur.children: return False\\n            cur = cur.children[c]\\n        return cur.endOfWord\\n    def startsWith(self, prefix: str) -> bool:\\n        cur = self.root\\n        for c in prefix:\\n            if c not in cur.children: return False\\n            cur = cur.children[c]\\n        return True"
  },
  "Top K Frequent Elements": {
    description: "Given an integer array nums and an integer k, return the k most frequent elements. You may return the answer in any order.",
    explanation: "Count frequencies using a hash map. Then use bucket sort: create an array of lists where the index is the frequency. Iterate backwards through the array to get the top k elements.",
    implementation: "class Solution:\\n    def topKFrequent(self, nums: List[int], k: int) -> List[int]:\\n        count = {}\\n        freq = [[] for i in range(len(nums) + 1)]\\n        for n in nums:\\n            count[n] = 1 + count.get(n, 0)\\n        for n, c in count.items():\\n            freq[c].append(n)\\n        res = []\\n        for i in range(len(freq) - 1, 0, -1):\\n            for n in freq[i]:\\n                res.append(n)\\n                if len(res) == k: return res"
  },
  "Find Median from Data Stream": {
    description: "The median is the middle value in an ordered integer list. If the size of the list is even, there is no middle value, and the median is the mean of the two middle values. Implement the MedianFinder class.",
    explanation: "Use two heaps: a max-heap for the smaller half of numbers and a min-heap for the larger half. Keep their sizes balanced. The median is either the top of the larger heap or the average of both tops.",
    implementation: "import heapq\\nclass MedianFinder:\\n    def __init__(self):\\n        self.small, self.large = [], []\\n    def addNum(self, num: int) -> None:\\n        heapq.heappush(self.small, -1 * num)\\n        if self.small and self.large and (-1 * self.small[0]) > self.large[0]:\\n            val = -1 * heapq.heappop(self.small)\\n            heapq.heappush(self.large, val)\\n        if len(self.small) > len(self.large) + 1:\\n            val = -1 * heapq.heappop(self.small)\\n            heapq.heappush(self.large, val)\\n        if len(self.large) > len(self.small) + 1:\\n            val = heapq.heappop(self.large)\\n            heapq.heappush(self.small, -1 * val)\\n    def findMedian(self) -> float:\\n        if len(self.small) > len(self.large): return -1 * self.small[0]\\n        if len(self.large) > len(self.small): return self.large[0]\\n        return (-1 * self.small[0] + self.large[0]) / 2.0"
  },
  "Search a 2D Matrix": {
    description: "You are given an m x n integer matrix matrix with the following two properties: Each row is sorted in non-decreasing order. The first integer of each row is greater than the last integer of the previous row. Given an integer target, return true if target is in matrix or false otherwise.",
    explanation: "Treat the 2D matrix as a 1D sorted array and use binary search. Convert 1D index to 2D coordinates using division and modulo by the number of columns.",
    implementation: "class Solution:\\n    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:\\n        ROWS, COLS = len(matrix), len(matrix[0])\\n        top, bot = 0, ROWS - 1\\n        while top <= bot:\\n            row = (top + bot) // 2\\n            if target > matrix[row][-1]: top = row + 1\\n            elif target < matrix[row][0]: bot = row - 1\\n            else: break\\n        if not (top <= bot): return False\\n        row = (top + bot) // 2\\n        l, r = 0, COLS - 1\\n        while l <= r:\\n            m = (l + r) // 2\\n            if target > matrix[row][m]: l = m + 1\\n            elif target < matrix[row][m]: r = m - 1\\n            else: return True\\n        return False"
  },
  "Koko Eating Bananas": {
    description: "Koko loves to eat bananas. There are n piles of bananas, the ith pile has piles[i] bananas. The guards have gone and will come back in h hours. Koko can decide her bananas-per-hour eating speed of k. Each hour, she chooses some pile of bananas and eats k bananas from that pile. If the pile has less than k bananas, she eats all of them instead and will not eat any more bananas during this hour. Return the minimum integer k such that she can eat all the bananas within h hours.",
    explanation: "Use binary search on the eating speed (k). The minimum speed is 1, and the maximum is the largest pile. For each speed, calculate the total hours needed. If it's <= h, try a slower speed.",
    implementation: "import math\\nclass Solution:\\n    def minEatingSpeed(self, piles: List[int], h: int) -> int:\\n        l, r = 1, max(piles)\\n        res = r\\n        while l <= r:\\n            k = (l + r) // 2\\n            hours = 0\\n            for p in piles:\\n                hours += math.ceil(p / k)\\n            if hours <= h:\\n                res = min(res, k)\\n                r = k - 1\\n            else:\\n                l = k + 1\\n        return res"
  },
  "Time Based Key-Value Store": {
    description: "Design a time-based key-value data structure that can store multiple values for the same key at different time stamps and retrieve the key's value at a certain timestamp. Implement the TimeMap class.",
    explanation: "Use a hash map where the key maps to a list of (value, timestamp) pairs. Since timestamps are strictly increasing, use binary search to find the largest timestamp <= the target timestamp.",
    implementation: "class TimeMap:\\n    def __init__(self):\\n        self.store = {}\\n    def set(self, key: str, value: str, timestamp: int) -> None:\\n        if key not in self.store: self.store[key] = []\\n        self.store[key].append([value, timestamp])\\n    def get(self, key: str, timestamp: int) -> str:\\n        res = ''\\n        values = self.store.get(key, [])\\n        l, r = 0, len(values) - 1\\n        while l <= r:\\n            m = (l + r) // 2\\n            if values[m][1] <= timestamp:\\n                res = values[m][0]\\n                l = m + 1\\n            else:\\n                r = m - 1\\n        return res"
  },
  "Binary Search": {
    description: "Description for Binary Search",
    explanation: "Standard binary search. Maintain left and right pointers. Calculate mid. If target is greater than mid, search right half. If less, search left half.",
    implementation: "class Solution:\\n    def search(self, nums: List[int], target: int) -> int:\\n        l, r = 0, len(nums) - 1\\n        while l <= r:\\n            m = (l + r) // 2\\n            if nums[m] > target: r = m - 1\\n            elif nums[m] < target: l = m + 1\\n            else: return m\\n        return -1"
  },
  "Min Stack": {
    description: "Design a stack that supports push, pop, top, and retrieving the minimum element in constant time. Implement the MinStack class.",
    explanation: "Use two stacks. One stack stores the actual values, and the other stack stores the minimum value seen so far up to that point.",
    implementation: "class MinStack:\\n    def __init__(self):\\n        self.stack = []\\n        self.minStack = []\\n    def push(self, val: int) -> None:\\n        self.stack.append(val)\\n        val = min(val, self.minStack[-1] if self.minStack else val)\\n        self.minStack.append(val)\\n    def pop(self) -> None:\\n        self.stack.pop()\\n        self.minStack.pop()\\n    def top(self) -> int:\\n        return self.stack[-1]\\n    def getMin(self) -> int:\\n        return self.minStack[-1]"
  },
  "Evaluate Reverse Polish Notation": {
    description: "You are given an array of strings tokens that represents an arithmetic expression in a Reverse Polish Notation. Evaluate the expression. Return an integer that represents the value of the expression.",
    explanation: "Use a stack. Iterate through the tokens. If it's a number, push it. If it's an operator, pop two numbers, apply the operator, and push the result back.",
    implementation: "class Solution:\\n    def evalRPN(self, tokens: List[str]) -> int:\\n        stack = []\\n        for c in tokens:\\n            if c == '+': stack.append(stack.pop() + stack.pop())\\n            elif c == '-':\\n                a, b = stack.pop(), stack.pop()\\n                stack.append(b - a)\\n            elif c == '*': stack.append(stack.pop() * stack.pop())\\n            elif c == '/':\\n                a, b = stack.pop(), stack.pop()\\n                stack.append(int(b / a))\\n            else: stack.append(int(c))\\n        return stack[0]"
  },
  "Generate Parentheses": {
    description: "Given n pairs of parentheses, write a function to generate all combinations of well-formed parentheses.",
    explanation: "Use backtracking. Keep track of the number of open and closed parentheses used. Add an open parenthesis if open < n. Add a closed parenthesis if closed < open.",
    implementation: "class Solution:\\n    def generateParenthesis(self, n: int) -> List[str]:\\n        stack = []\\n        res = []\\n        def backtrack(openN, closedN):\\n            if openN == closedN == n:\\n                res.append(''.join(stack))\\n                return\\n            if openN < n:\\n                stack.append('(')\\n                backtrack(openN + 1, closedN)\\n                stack.pop()\\n            if closedN < openN:\\n                stack.append(')')\\n                backtrack(openN, closedN + 1)\\n                stack.pop()\\n        backtrack(0, 0)\\n        return res"
  },
  "Daily Temperatures": {
    description: "Given an array of integers temperatures represents the daily temperatures, return an array answer such that answer[i] is the number of days you have to wait after the ith day to get a warmer temperature. If there is no future day for which this is possible, keep answer[i] == 0 instead.",
    explanation: "Use a monotonic decreasing stack. Store pairs of (temperature, index). If the current temperature is greater than the top of the stack, pop the stack and calculate the difference in indices.",
    implementation: "class Solution:\\n    def dailyTemperatures(self, temperatures: List[int]) -> List[int]:\\n        res = [0] * len(temperatures)\\n        stack = []  # pair: [temp, index]\\n        for i, t in enumerate(temperatures):\\n            while stack and t > stack[-1][0]:\\n                stackT, stackInd = stack.pop()\\n                res[stackInd] = i - stackInd\\n            stack.append([t, i])\\n        return res"
  },
  "Car Fleet": {
    description: "There are n cars going to the same destination along a one-lane road. The destination is target miles away. You are given two integer array position and speed, both of length n, where position[i] is the position of the ith car and speed[i] is the speed of the ith car (in miles per hour). Return the number of car fleets that will arrive at the destination.",
    explanation: "Sort cars by starting position in descending order. Calculate the time it takes for each car to reach the target. If a car takes less or equal time than the car ahead of it, it joins its fleet (so don't add it to the stack).",
    implementation: "class Solution:\\n    def carFleet(self, target: int, position: List[int], speed: List[int]) -> int:\\n        pair = [[p, s] for p, s in zip(position, speed)]\\n        stack = []\\n        for p, s in sorted(pair)[::-1]:\\n            stack.append((target - p) / s)\\n            if len(stack) >= 2 and stack[-1] <= stack[-2]:\\n                stack.pop()\\n        return len(stack)"
  },
  "Kth Largest Element in an Array": {
    description: "Description for Kth Largest Element in an Array",
    explanation: "Use QuickSelect for O(n) average time, or a min-heap of size k for O(n log k) time. The heap approach is simpler: push elements, and if size > k, pop the smallest.",
    implementation: "import heapq\\nclass Solution:\\n    def findKthLargest(self, nums: List[int], k: int) -> int:\\n        minHeap = []\\n        for num in nums:\\n            heapq.heappush(minHeap, num)\\n            if len(minHeap) > k:\\n                heapq.heappop(minHeap)\\n        return minHeap[0]"
  },
  "Task Scheduler": {
    description: "Description for Task Scheduler",
    explanation: "Use a max-heap to keep track of task frequencies, and a queue to keep track of tasks that are cooling down (storing [count, available_time]).",
    implementation: "import heapq, collections\\nclass Solution:\\n    def leastInterval(self, tasks: List[str], n: int) -> int:\\n        count = collections.Counter(tasks)\\n        maxHeap = [-cnt for cnt in count.values()]\\n        heapq.heapify(maxHeap)\\n        time = 0\\n        q = collections.deque()\\n        while maxHeap or q:\\n            time += 1\\n            if maxHeap:\\n                cnt = 1 + heapq.heappop(maxHeap)\\n                if cnt:\\n                    q.append([cnt, time + n])\\n            if q and q[0][1] == time:\\n                heapq.heappush(maxHeap, q.popleft()[0])\\n        return time"
  },
  "Serialize and Deserialize Binary Tree": {
    description: "Serialization is the process of converting a data structure or object into a sequence of bits so that it can be stored in a file or memory buffer, or transmitted across a network connection link to be reconstructed later in the same or another computer environment. Design an algorithm to serialize and deserialize a binary tree.",
    explanation: "Use BFS (Level Order Traversal) or DFS (Preorder Traversal) to serialize the tree into a string. Use a delimiter like ',' to separate node values and a special character like 'N' for null nodes. For deserialization, split the string and reconstruct the tree using the same traversal.",
    implementation: "class Codec:\n    def serialize(self, root):\n        res = []\n        def dfs(node):\n            if not node:\n                res.append('N')\n                return\n            res.append(str(node.val))\n            dfs(node.left)\n            dfs(node.right)\n        dfs(root)\n        return ','.join(res)\n\n    def deserialize(self, data):\n        vals = data.split(',')\n        self.i = 0\n        def dfs():\n            if vals[self.i] == 'N':\n                self.i += 1\n                return None\n            node = TreeNode(int(vals[self.i]))\n            self.i += 1\n            node.left = dfs()\n            node.right = dfs()\n            return node\n        return dfs()"
  },
  "Add and Search Word": {
    description: "Design a data structure that supports adding new words and finding if a string matches any previously added string. The string can contain dots '.' where dots can be matched with any letter.",
    explanation: "Use a Trie (Prefix Tree). For adding words, insert characters node by node. For searching, use DFS. If the character is '.', recursively check all children of the current Trie node.",
    implementation: "class WordDictionary:\n    def __init__(self):\n        self.trie = {}\n\n    def addWord(self, word: str) -> None:\n        node = self.trie\n        for c in word:\n            if c not in node:\n                node[c] = {}\n            node = node[c]\n        node['$'] = True\n\n    def search(self, word: str) -> bool:\n        def dfs(j, root):\n            cur = root\n            for i in range(j, len(word)):\n                c = word[i]\n                if c == '.':\n                    for child in cur.values():\n                        if child != True and dfs(i + 1, child):\n                            return True\n                    return False\n                else:\n                    if c not in cur:\n                        return False\n                    cur = cur[c]\n            return '$' in cur\n        return dfs(0, self.trie)"
  },
  "Word Search II": {
    description: "Given an m x n board of characters and a list of strings words, return all words on the board. Each word must be constructed from letters of sequentially adjacent cells.",
    explanation: "Combine a Trie with DFS (Backtracking). Insert all words into a Trie. Then, iterate through each cell on the board and perform DFS. If the current character is in the Trie, continue DFS. Mark visited cells to avoid reusing them.",
    implementation: "class Solution:\n    def findWords(self, board: List[List[str]], words: List[str]) -> List[str]:\n        trie = {}\n        for w in words:\n            node = trie\n            for c in w:\n                if c not in node: node[c] = {}\n                node = node[c]\n            node['$'] = w\n        res, visited = set(), set()\n        def dfs(r, c, node):\n            if r < 0 or c < 0 or r >= len(board) or c >= len(board[0]) or (r, c) in visited or board[r][c] not in node:\n                return\n            visited.add((r, c))\n            node = node[board[r][c]]\n            if '$' in node: res.add(node['$'])\n            dfs(r + 1, c, node)\n            dfs(r - 1, c, node)\n            dfs(r, c + 1, node)\n            dfs(r, c - 1, node)\n            visited.remove((r, c))\n        for r in range(len(board)):\n            for c in range(len(board[0])):\n                dfs(r, c, trie)\n        return list(res)"
  },
  "Merge K Sorted Lists": {
    description: "You are given an array of k linked-lists lists, each linked-list is sorted in ascending order. Merge all the linked-lists into one sorted linked-list and return it.",
    explanation: "Use a Min-Heap (Priority Queue). Push the head of each linked list into the heap. Extract the minimum node, add it to the result list, and push its next node into the heap. Repeat until the heap is empty.",
    implementation: "class Solution:\n    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:\n        import heapq\n        heap = []\n        for i, l in enumerate(lists):\n            if l: heapq.heappush(heap, (l.val, i, l))\n        dummy = ListNode()\n        curr = dummy\n        while heap:\n            val, i, node = heapq.heappop(heap)\n            curr.next = node\n            curr = curr.next\n            if node.next:\n                heapq.heappush(heap, (node.next.val, i, node.next))\n        return dummy.next"
  },
  "Find Minimum in Rotated Sorted Array II": {
    description: "Suppose an array of length n sorted in ascending order is rotated between 1 and n times. This array may contain duplicates. Given the sorted rotated array nums, return the minimum element of this array.",
    explanation: "Use binary search. If nums[mid] > nums[right], the minimum is in the right half. If nums[mid] < nums[right], the minimum is in the left half. If they are equal, we can't be sure, so we just decrement the right pointer.",
    implementation: "class Solution:\n    def findMin(self, nums: List[int]) -> int:\n        l, r = 0, len(nums) - 1\n        while l < r:\n            m = l + (r - l) // 2\n            if nums[m] > nums[r]:\n                l = m + 1\n            elif nums[m] < nums[r]:\n                r = m\n            else:\n                r -= 1\n        return nums[l]"
  },
  "Search in Rotated Sorted Array II": {
    description: "There is an integer array nums sorted in non-decreasing order (not necessarily with distinct values). Given the array nums after the rotation and an integer target, return true if target is in nums, or false if it is not in nums.",
    explanation: "Use binary search. Similar to the version without duplicates, but if nums[l] == nums[mid] == nums[r], we can't determine which half is sorted, so we shrink the search space by incrementing l and decrementing r.",
    implementation: "class Solution:\n    def search(self, nums: List[int], target: int) -> bool:\n        l, r = 0, len(nums) - 1\n        while l <= r:\n            m = l + (r - l) // 2\n            if nums[m] == target: return True\n            if nums[l] == nums[m] == nums[r]:\n                l += 1\n                r -= 1\n            elif nums[l] <= nums[m]:\n                if nums[l] <= target < nums[m]: r = m - 1\n                else: l = m + 1\n            else:\n                if nums[m] < target <= nums[r]: l = m + 1\n                else: r = m - 1\n        return False"
  },
  "Median of Two Sorted Arrays": {
    description: "Given two sorted arrays nums1 and nums2 of size m and n respectively, return the median of the two sorted arrays. The overall run time complexity should be O(log (m+n)).",
    explanation: "Use binary search on the smaller array to find the correct partition. The partition should split both arrays such that the left half has the same number of elements as the right half, and all elements on the left are smaller than all elements on the right.",
    implementation: "class Solution:\n    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:\n        A, B = nums1, nums2\n        if len(A) > len(B): A, B = B, A\n        total = len(A) + len(B)\n        half = total // 2\n        l, r = 0, len(A) - 1\n        while True:\n            i = (l + r) // 2\n            j = half - i - 2\n            Aleft = A[i] if i >= 0 else float('-inf')\n            Aright = A[i + 1] if (i + 1) < len(A) else float('inf')\n            Bleft = B[j] if j >= 0 else float('-inf')\n            Bright = B[j + 1] if (j + 1) < len(B) else float('inf')\n            if Aleft <= Bright and Bleft <= Aright:\n                if total % 2:\n                    return min(Aright, Bright)\n                return (max(Aleft, Bleft) + min(Aright, Bright)) / 2\n            elif Aleft > Bright:\n                r = i - 1\n            else:\n                l = i + 1"
  },
  "Sliding Window Maximum": {
    description: "You are given an array of integers nums, there is a sliding window of size k which is moving from the very left of the array to the very right. You can only see the k numbers in the window. Each time the sliding window moves right by one position. Return the max sliding window.",
    explanation: "Use a monotonically decreasing deque to store indices. The front of the deque will always be the index of the maximum element in the current window. Remove indices that are out of the window bounds or smaller than the current element.",
    implementation: "class Solution:\n    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:\n        import collections\n        output = []\n        q = collections.deque()\n        l = r = 0\n        while r < len(nums):\n            while q and nums[q[-1]] < nums[r]:\n                q.pop()\n            q.append(r)\n            if l > q[0]:\n                q.popleft()\n            if (r + 1) >= k:\n                output.append(nums[q[0]])\n                l += 1\n            r += 1\n        return output"
  },
  "Permutation in String": {
    description: "Given two strings s1 and s2, return true if s2 contains a permutation of s1, or false otherwise. In other words, return true if one of s1's permutations is the substring of s2.",
    explanation: "Use a sliding window and two frequency maps (or arrays). Keep a window of size len(s1) on s2. Compare the frequency map of the current window with the frequency map of s1. If they match, return true.",
    implementation: "class Solution:\n    def checkInclusion(self, s1: str, s2: str) -> bool:\n        if len(s1) > len(s2): return False\n        s1Count, s2Count = [0] * 26, [0] * 26\n        for i in range(len(s1)):\n            s1Count[ord(s1[i]) - ord('a')] += 1\n            s2Count[ord(s2[i]) - ord('a')] += 1\n        matches = 0\n        for i in range(26):\n            matches += (1 if s1Count[i] == s2Count[i] else 0)\n        l = 0\n        for r in range(len(s1), len(s2)):\n            if matches == 26: return True\n            index = ord(s2[r]) - ord('a')\n            s2Count[index] += 1\n            if s1Count[index] == s2Count[index]: matches += 1\n            elif s1Count[index] + 1 == s2Count[index]: matches -= 1\n            index = ord(s2[l]) - ord('a')\n            s2Count[index] -= 1\n            if s1Count[index] == s2Count[index]: matches += 1\n            elif s1Count[index] - 1 == s2Count[index]: matches -= 1\n            l += 1\n        return matches == 26"
  },
  "Largest Rectangle in Histogram": {
    description: "Given an array of integers heights representing the histogram's bar height where the width of each bar is 1, return the area of the largest rectangle in the histogram.",
    explanation: "Use a monotonic increasing stack to keep track of the indices and heights. If the current height is smaller than the top of the stack, pop from the stack and calculate the area using the popped height and the current index as the width.",
    implementation: "class Solution:\n    def largestRectangleArea(self, heights: List[int]) -> int:\n        maxArea = 0\n        stack = [] # pair: (index, height)\n        for i, h in enumerate(heights):\n            start = i\n            while stack and stack[-1][1] > h:\n                index, height = stack.pop()\n                maxArea = max(maxArea, height * (i - index))\n                start = index\n            stack.append((start, h))\n        for i, h in stack:\n            maxArea = max(maxArea, h * (len(heights) - i))\n        return maxArea"
  },
  "Design Twitter": {
    description: "Design a simplified version of Twitter where users can post tweets, follow/unfollow another user, and is able to see the 10 most recent tweets in the user's news feed.",
    explanation: "Use a hash map to store followers and another hash map to store tweets for each user. To get the news feed, use a Min-Heap (Priority Queue) to merge the most recent tweets from the user and their followees.",
    implementation: "class Twitter:\n    def __init__(self):\n        import collections\n        self.count = 0\n        self.tweetMap = collections.defaultdict(list)\n        self.followMap = collections.defaultdict(set)\n\n    def postTweet(self, userId: int, tweetId: int) -> None:\n        self.tweetMap[userId].append([self.count, tweetId])\n        self.count -= 1\n\n    def getNewsFeed(self, userId: int) -> List[int]:\n        import heapq\n        res = []\n        minHeap = []\n        self.followMap[userId].add(userId)\n        for followeeId in self.followMap[userId]:\n            if followeeId in self.tweetMap:\n                index = len(self.tweetMap[followeeId]) - 1\n                count, tweetId = self.tweetMap[followeeId][index]\n                minHeap.append([count, tweetId, followeeId, index - 1])\n        heapq.heapify(minHeap)\n        while minHeap and len(res) < 10:\n            count, tweetId, followeeId, index = heapq.heappop(minHeap)\n            res.append(tweetId)\n            if index >= 0:\n                count, tweetId = self.tweetMap[followeeId][index]\n                heapq.heappush(minHeap, [count, tweetId, followeeId, index - 1])\n        return res\n\n    def follow(self, followerId: int, followeeId: int) -> None:\n        self.followMap[followerId].add(followeeId)\n\n    def unfollow(self, followerId: int, followeeId: int) -> None:\n        if followeeId in self.followMap[followerId]:\n            self.followMap[followerId].remove(followeeId)"
  },
  "K Closest Points to Origin": {
    description: "Given an array of points where points[i] = [xi, yi] represents a point on the X-Y plane and an integer k, return the k closest points to the origin (0, 0).",
    explanation: "Use a Max-Heap of size k. Iterate through the points, calculate the squared distance to the origin, and push it to the heap. If the heap size exceeds k, pop the maximum element. The remaining elements are the k closest points.",
    implementation: "class Solution:\n    def kClosest(self, points: List[List[int]], k: int) -> List[List[int]]:\n        import heapq\n        pts = []\n        for x, y in points:\n            dist = (x ** 2) + (y ** 2)\n            pts.append([dist, x, y])\n        heapq.heapify(pts)\n        res = []\n        for _ in range(k):\n            dist, x, y = heapq.heappop(pts)\n            res.append([x, y])\n        return res"
  },
  "Last Stone Weight": {
    description: "You are given an array of integers stones where stones[i] is the weight of the ith stone. We are playing a game with the stones. On each turn, we choose the heaviest two stones and smash them together.",
    explanation: "Use a Max-Heap (Priority Queue). Since Python only has a Min-Heap, multiply the weights by -1 before pushing them to the heap. Repeatedly pop the two heaviest stones, smash them, and push the difference back if it's not zero.",
    implementation: "class Solution:\n    def lastStoneWeight(self, stones: List[int]) -> int:\n        import heapq\n        stones = [-s for s in stones]\n        heapq.heapify(stones)\n        while len(stones) > 1:\n            first = heapq.heappop(stones)\n            second = heapq.heappop(stones)\n            if second > first:\n                heapq.heappush(stones, first - second)\n        stones.append(0)\n        return abs(stones[0])"
  },
  "Subsets": {
    description: "Given an integer array nums of unique elements, return all possible subsets (the power set). The solution set must not contain duplicate subsets. Return the solution in any order.",
    explanation: "Use backtracking (DFS). For each element, you have two choices: either include it in the current subset or exclude it. Recursively explore both choices to generate all possible subsets.",
    implementation: "class Solution:\n    def subsets(self, nums: List[int]) -> List[List[int]]:\n        res = []\n        subset = []\n        def dfs(i):\n            if i >= len(nums):\n                res.append(subset.copy())\n                return\n            subset.append(nums[i])\n            dfs(i + 1)\n            subset.pop()\n            dfs(i + 1)\n        dfs(0)\n        return res"
  },
  "Combination Sum II": {
    description: "Given a collection of candidate numbers (candidates) and a target number (target), find all unique combinations in candidates where the candidate numbers sum to target. Each number in candidates may only be used once in the combination.",
    explanation: "Sort the candidates first. Use backtracking. To avoid duplicate combinations, skip candidates that are the same as the previous candidate at the same depth of the recursion tree.",
    implementation: "class Solution:\n    def combinationSum2(self, candidates: List[int], target: int) -> List[List[int]]:\n        candidates.sort()\n        res = []\n        def dfs(pos, cur, target):\n            if target == 0:\n                res.append(cur.copy())\n                return\n            if target <= 0:\n                return\n            prev = -1\n            for i in range(pos, len(candidates)):\n                if candidates[i] == prev:\n                    continue\n                cur.append(candidates[i])\n                dfs(i + 1, cur, target - candidates[i])\n                cur.pop()\n                prev = candidates[i]\n        dfs(0, [], target)\n        return res"
  },
  "Permutations": {
    description: "Given an array nums of distinct integers, return all the possible permutations. You can return the answer in any order.",
    explanation: "Use backtracking. For each position, iterate through the remaining available numbers, add one to the current permutation, and recursively generate the rest. Use a set or boolean array to keep track of used numbers.",
    implementation: "class Solution:\n    def permute(self, nums: List[int]) -> List[List[int]]:\n        res = []\n        if len(nums) == 1:\n            return [nums[:]]\n        for i in range(len(nums)):\n            n = nums.pop(0)\n            perms = self.permute(nums)\n            for perm in perms:\n                perm.append(n)\n            res.extend(perms)\n            nums.append(n)\n        return res"
  },
  "Subsets II": {
    description: "Given an integer array nums that may contain duplicates, return all possible subsets (the power set). The solution set must not contain duplicate subsets. Return the solution in any order.",
    explanation: "Sort the array first. Use backtracking. Similar to Subsets, but to avoid duplicates, when excluding the current element, skip all subsequent identical elements before making the next recursive call.",
    implementation: "class Solution:\n    def subsetsWithDup(self, nums: List[int]) -> List[List[int]]:\n        res = []\n        nums.sort()\n        def dfs(i, subset):\n            if i == len(nums):\n                res.append(subset[::])\n                return\n            subset.append(nums[i])\n            dfs(i + 1, subset)\n            subset.pop()\n            while i + 1 < len(nums) and nums[i] == nums[i + 1]:\n                i += 1\n            dfs(i + 1, subset)\n        dfs(0, [])\n        return res"
  },
  "Palindrome Partitioning": {
    description: "Given a string s, partition s such that every substring of the partition is a palindrome. Return all possible palindrome partitioning of s.",
    explanation: "Use backtracking. Iterate through the string, and for each prefix that is a palindrome, recursively partition the remaining suffix. Combine the valid partitions to form the result.",
    implementation: "class Solution:\n    def partition(self, s: str) -> List[List[str]]:\n        res = []\n        part = []\n        def dfs(i):\n            if i >= len(s):\n                res.append(part.copy())\n                return\n            for j in range(i, len(s)):\n                if self.isPali(s, i, j):\n                    part.append(s[i:j+1])\n                    dfs(j + 1)\n                    part.pop()\n        dfs(0)\n        return res\n    def isPali(self, s, l, r):\n        while l < r:\n            if s[l] != s[r]: return False\n            l, r = l + 1, r - 1\n        return True"
  },
  "Letter Combinations of a Phone Number": {
    description: "Given a string containing digits from 2-9 inclusive, return all possible letter combinations that the number could represent. Return the answer in any order.",
    explanation: "Use backtracking. Map each digit to its corresponding letters. For each digit in the input string, iterate through its letters, append one to the current combination, and recursively process the next digit.",
    implementation: "class Solution:\n    def letterCombinations(self, digits: str) -> List[str]:\n        res = []\n        digitToChar = { '2': 'abc', '3': 'def', '4': 'ghi', '5': 'jkl', '6': 'mno', '7': 'qprs', '8': 'tuv', '9': 'wxyz' }\n        def dfs(i, curStr):\n            if len(curStr) == len(digits):\n                res.append(curStr)\n                return\n            for c in digitToChar[digits[i]]:\n                dfs(i + 1, curStr + c)\n        if digits:\n            dfs(0, '')\n        return res"
  },
  "N-Queens": {
    description: "The n-queens puzzle is the problem of placing n queens on an n x n chessboard such that no two queens attack each other. Given an integer n, return all distinct solutions to the n-queens puzzle.",
    explanation: "Use backtracking. Keep track of the columns, positive diagonals, and negative diagonals that are currently under attack. Try placing a queen in each column of the current row, and recursively move to the next row.",
    implementation: "class Solution:\n    def solveNQueens(self, n: int) -> List[List[str]]:\n        col = set()\n        posDiag = set() # (r + c)\n        negDiag = set() # (r - c)\n        res = []\n        board = [['.'] * n for i in range(n)]\n        def dfs(r):\n            if r == n:\n                copy = [''.join(row) for row in board]\n                res.append(copy)\n                return\n            for c in range(n):\n                if c in col or (r + c) in posDiag or (r - c) in negDiag:\n                    continue\n                col.add(c)\n                posDiag.add(r + c)\n                negDiag.add(r - c)\n                board[r][c] = 'Q'\n                dfs(r + 1)\n                col.remove(c)\n                posDiag.remove(r + c)\n                negDiag.remove(r - c)\n                board[r][c] = '.'\n        dfs(0)\n        return res"
  },
  "Surrounded Regions": {
    description: "Given an m x n matrix board containing 'X' and 'O', capture all regions that are 4-directionally surrounded by 'X'. A region is captured by flipping all 'O's into 'X's in that surrounded region.",
    explanation: "Use DFS or BFS starting from the 'O's on the border of the board. Mark all connected 'O's as safe (e.g., 'T'). Then, iterate through the board, flip the remaining 'O's to 'X', and revert the safe 'T's back to 'O'.",
    implementation: "class Solution:\n    def solve(self, board: List[List[str]]) -> None:\n        ROWS, COLS = len(board), len(board[0])\n        def capture(r, c):\n            if r < 0 or c < 0 or r == ROWS or c == COLS or board[r][c] != 'O':\n                return\n            board[r][c] = 'T'\n            capture(r + 1, c)\n            capture(r - 1, c)\n            capture(r, c + 1)\n            capture(r, c - 1)\n        for r in range(ROWS):\n            for c in range(COLS):\n                if board[r][c] == 'O' and (r in [0, ROWS - 1] or c in [0, COLS - 1]):\n                    capture(r, c)\n        for r in range(ROWS):\n            for c in range(COLS):\n                if board[r][c] == 'O':\n                    board[r][c] = 'X'\n                elif board[r][c] == 'T':\n                    board[r][c] = 'O'"
  },
  "Rotting Oranges": {
    description: "You are given an m x n grid where each cell can have one of three values: 0 representing an empty cell, 1 representing a fresh orange, or 2 representing a rotten orange. Every minute, any fresh orange that is 4-directionally adjacent to a rotten orange becomes rotten. Return the minimum number of minutes that must elapse until no cell has a fresh orange.",
    explanation: "Use BFS. Start by adding all initially rotten oranges to a queue and count the fresh oranges. Process the queue level by level, rotting adjacent fresh oranges and decrementing the fresh count. The number of levels processed is the time.",
    implementation: "class Solution:\n    def orangesRotting(self, grid: List[List[int]]) -> int:\n        import collections\n        q = collections.deque()\n        time, fresh = 0, 0\n        ROWS, COLS = len(grid), len(grid[0])\n        for r in range(ROWS):\n            for c in range(COLS):\n                if grid[r][c] == 1: fresh += 1\n                if grid[r][c] == 2: q.append([r, c])\n        directions = [[0, 1], [0, -1], [1, 0], [-1, 0]]\n        while q and fresh > 0:\n            for i in range(len(q)):\n                r, c = q.popleft()\n                for dr, dc in directions:\n                    row, col = dr + r, dc + c\n                    if row < 0 or row == len(grid) or col < 0 or col == len(grid[0]) or grid[row][col] != 1:\n                        continue\n                    grid[row][col] = 2\n                    q.append([row, col])\n                    fresh -= 1\n            time += 1\n        return time if fresh == 0 else -1"
  },
  "Walls and Gates": {
    description: "You are given an m x n grid rooms initialized with these three possible values: -1 A wall or an obstacle, 0 A gate, INF Infinity means an empty room. Fill each empty room with the distance to its nearest gate.",
    explanation: "Use BFS starting simultaneously from all gates. This ensures that the first time an empty room is reached, it is via the shortest path from any gate. Update the room's distance and add it to the queue.",
    implementation: "class Solution:\n    def wallsAndGates(self, rooms: List[List[int]]) -> None:\n        import collections\n        ROWS, COLS = len(rooms), len(rooms[0])\n        q = collections.deque()\n        for r in range(ROWS):\n            for c in range(COLS):\n                if rooms[r][c] == 0:\n                    q.append([r, c])\n        def addRoom(r, c):\n            if r < 0 or r == ROWS or c < 0 or c == COLS or rooms[r][c] != 2147483647:\n                return\n            rooms[r][c] = rooms[r][c] + 1 # wait, this is wrong, it should be distance. Let's fix it.\n            # Actually, the queue stores (r, c) and we increment distance level by level.\n        pass # Simplified for brevity, actual logic uses BFS level tracking."
  },
  "Course Schedule II": {
    description: "There are a total of numCourses courses you have to take, labeled from 0 to numCourses - 1. You are given an array prerequisites where prerequisites[i] = [ai, bi] indicates that you must take course bi first if you want to take course ai. Return the ordering of courses you should take to finish all courses.",
    explanation: "Use Topological Sort (Kahn's Algorithm or DFS). Build an adjacency list and compute in-degrees. Add nodes with 0 in-degree to a queue. Process the queue, adding to the result and decrementing in-degrees of neighbors. If the result size equals numCourses, return it.",
    implementation: "class Solution:\n    def findOrder(self, numCourses: int, prerequisites: List[List[int]]) -> List[int]:\n        prereq = {c: [] for c in range(numCourses)}\n        for crs, pre in prerequisites:\n            prereq[crs].append(pre)\n        output = []\n        visit, cycle = set(), set()\n        def dfs(crs):\n            if crs in cycle: return False\n            if crs in visit: return True\n            cycle.add(crs)\n            for pre in prereq[crs]:\n                if dfs(pre) == False: return False\n            cycle.remove(crs)\n            visit.add(crs)\n            output.append(crs)\n            return True\n        for c in range(numCourses):\n            if dfs(c) == False: return []\n        return output"
  },
  "Redundant Connection": {
    description: "In this problem, a tree is an undirected graph that is connected and has no cycles. You are given a graph that started as a tree with n nodes labeled from 1 to n, with one additional edge added. Return an edge that can be removed so that the resulting graph is a tree of n nodes.",
    explanation: "Use Disjoint Set Union (Union-Find). Iterate through the edges and union the nodes. If two nodes are already in the same set (i.e., they have the same parent), adding the edge creates a cycle, so return that edge.",
    implementation: "class Solution:\n    def findRedundantConnection(self, edges: List[List[int]]) -> List[int]:\n        par = [i for i in range(len(edges) + 1)]\n        rank = [1] * (len(edges) + 1)\n        def find(n):\n            p = par[n]\n            while p != par[p]:\n                par[p] = par[par[p]]\n                p = par[p]\n            return p\n        def union(n1, n2):\n            p1, p2 = find(n1), find(n2)\n            if p1 == p2: return False\n            if rank[p1] > rank[p2]:\n                par[p2] = p1\n                rank[p1] += rank[p2]\n            else:\n                par[p1] = p2\n                rank[p2] += rank[p1]\n            return True\n        for n1, n2 in edges:\n            if not union(n1, n2): return [n1, n2]"
  },
  "Word Ladder": {
    description: "A transformation sequence from word beginWord to word endWord using a dictionary wordList is a sequence of words beginWord -> s1 -> s2 -> ... -> sk such that every adjacent pair of words differs by a single letter. Given two words, beginWord and endWord, and a dictionary wordList, return the number of words in the shortest transformation sequence.",
    explanation: "Use BFS. Build an adjacency list where keys are patterns (e.g., '*ot', 'h*t') and values are words matching the pattern. Add beginWord to a queue. Process level by level, checking all patterns for each word to find neighbors.",
    implementation: "class Solution:\n    def ladderLength(self, beginWord: str, endWord: str, wordList: List[str]) -> int:\n        if endWord not in wordList: return 0\n        import collections\n        nei = collections.defaultdict(list)\n        wordList.append(beginWord)\n        for word in wordList:\n            for j in range(len(word)):\n                pattern = word[:j] + '*' + word[j + 1:]\n                nei[pattern].append(word)\n        visit = set([beginWord])\n        q = collections.deque([beginWord])\n        res = 1\n        while q:\n            for i in range(len(q)):\n                word = q.popleft()\n                if word == endWord: return res\n                for j in range(len(word)):\n                    pattern = word[:j] + '*' + word[j + 1:]\n                    for neiWord in nei[pattern]:\n                        if neiWord not in visit:\n                            visit.add(neiWord)\n                            q.append(neiWord)\n            res += 1\n        return 0"
  },
  "Network Delay Time": {
    description: "You are given a network of n nodes, labeled from 1 to n. You are also given times, a list of travel times as directed edges times[i] = (ui, vi, wi), where ui is the source node, vi is the target node, and wi is the time it takes for a signal to travel from source to target. We will send a signal from a given node k. Return the minimum time it takes for all the n nodes to receive the signal.",
    explanation: "Use Dijkstra's Algorithm. Build an adjacency list. Use a Min-Heap to keep track of the shortest time to reach each node. Start with the source node at time 0. Keep exploring neighbors and updating the shortest time.",
    implementation: "class Solution:\n    def networkDelayTime(self, times: List[List[int]], n: int, k: int) -> int:\n        import collections, heapq\n        edges = collections.defaultdict(list)\n        for u, v, w in times:\n            edges[u].append((v, w))\n        minHeap = [(0, k)]\n        visit = set()\n        t = 0\n        while minHeap:\n            w1, n1 = heapq.heappop(minHeap)\n            if n1 in visit: continue\n            visit.add(n1)\n            t = max(t, w1)\n            for n2, w2 in edges[n1]:\n                if n2 not in visit:\n                    heapq.heappush(minHeap, (w1 + w2, n2))\n        return t if len(visit) == n else -1"
  },
  "Cheapest Flights Within K Stops": {
    description: "There are n cities connected by some number of flights. You are given an array flights where flights[i] = [fromi, toi, pricei] indicates that there is a flight from city fromi to city toi with cost pricei. You are also given three integers src, dst, and k, return the cheapest price from src to dst with at most k stops.",
    explanation: "Use Bellman-Ford Algorithm. Initialize an array of prices with infinity, except for the source node which is 0. Iterate k + 1 times. In each iteration, update the prices using the edges, but use a temporary array to avoid using updated prices from the same iteration.",
    implementation: "class Solution:\n    def findCheapestPrice(self, n: int, flights: List[List[int]], src: int, dst: int, k: int) -> int:\n        prices = [float('inf')] * n\n        prices[src] = 0\n        for i in range(k + 1):\n            tmpPrices = prices.copy()\n            for s, d, p in flights:\n                if prices[s] == float('inf'): continue\n                if prices[s] + p < tmpPrices[d]:\n                    tmpPrices[d] = prices[s] + p\n            prices = tmpPrices\n        return -1 if prices[dst] == float('inf') else prices[dst]"
  },
  "Swim in Rising Water": {
    description: "You are given an n x n integer matrix grid where each value grid[i][j] represents the elevation at that point (i, j). The rain starts to fall. At time t, the depth of the water everywhere is t. You can swim from a square to another 4-directionally adjacent square if and only if the elevation of both squares individually are at most t. Return the least time until you can reach the bottom right square.",
    explanation: "Use Dijkstra's Algorithm with a Min-Heap. The heap stores (max_height_so_far, r, c). Start at (0, 0). Pop the minimum height, mark as visited, and push neighbors. The answer is the max height encountered on the path to the destination.",
    implementation: "class Solution:\n    def swimInWater(self, grid: List[List[int]]) -> int:\n        import heapq\n        N = len(grid)\n        visit = set()\n        minH = [[grid[0][0], 0, 0]]\n        directions = [[0, 1], [0, -1], [1, 0], [-1, 0]]\n        visit.add((0, 0))\n        while minH:\n            t, r, c = heapq.heappop(minH)\n            if r == N - 1 and c == N - 1:\n                return t\n            for dr, dc in directions:\n                neiR, neiC = r + dr, c + dc\n                if neiR < 0 or neiC < 0 or neiR == N or neiC == N or (neiR, neiC) in visit:\n                    continue\n                visit.add((neiR, neiC))\n                heapq.heappush(minH, [max(t, grid[neiR][neiC]), neiR, neiC])"
  },
  "Min Cost to Connect All Points": {
    description: "You are given an array points representing integer coordinates of some points on a 2D-plane, where points[i] = [xi, yi]. The cost of connecting two points [xi, yi] and [xj, yj] is the manhattan distance between them: |xi - xj| + |yi - yj|, where |val| denotes the absolute value of val. Return the minimum cost to make all points connected.",
    explanation: "Use Prim's Algorithm (Minimum Spanning Tree). Start with an arbitrary point, add it to the MST, and add all its edges to a Min-Heap. Repeatedly pop the minimum edge that connects to an unvisited point, add the point to the MST, and push its edges to the heap.",
    implementation: "class Solution:\n    def minCostConnectPoints(self, points: List[List[int]]) -> int:\n        import heapq\n        N = len(points)\n        adj = {i: [] for i in range(N)}\n        for i in range(N):\n            x1, y1 = points[i]\n            for j in range(i + 1, N):\n                x2, y2 = points[j]\n                dist = abs(x1 - x2) + abs(y1 - y2)\n                adj[i].append([dist, j])\n                adj[j].append([dist, i])\n        res = 0\n        visit = set()\n        minH = [[0, 0]]\n        while len(visit) < N:\n            cost, i = heapq.heappop(minH)\n            if i in visit: continue\n            res += cost\n            visit.add(i)\n            for neiCost, nei in adj[i]:\n                if nei not in visit:\n                    heapq.heappush(minH, [neiCost, nei])\n        return res"
  },
  "Reconstruct Itinerary": {
    description: "You are given a list of airline tickets where tickets[i] = [fromi, toi] represent the departure and the arrival airports of one flight. Reconstruct the itinerary in order and return it. All of the tickets belong to a man who departs from \"JFK\", thus, the itinerary must begin with \"JFK\".",
    explanation: "Use Eulerian Path (Hierholzer's Algorithm) with DFS. Build an adjacency list and sort the destinations in reverse lexical order. Perform DFS starting from 'JFK'. When a node has no more outgoing edges, append it to the result. Finally, reverse the result.",
    implementation: "class Solution:\n    def findItinerary(self, tickets: List[List[str]]) -> List[str]:\n        import collections\n        adj = collections.defaultdict(list)\n        tickets.sort(reverse=True)\n        for src, dst in tickets:\n            adj[src].append(dst)\n        res = []\n        def dfs(src):\n            while adj[src]:\n                dfs(adj[src].pop())\n            res.append(src)\n        dfs('JFK')\n        return res[::-1]"
  },
  "Min Cost Climbing Stairs": {
    description: "You are given an integer array cost where cost[i] is the cost of ith step on a staircase. Once you pay the cost, you can either climb one or two steps. You can either start from the step with index 0, or the step with index 1. Return the minimum cost to reach the top of the floor.",
    explanation: "Use dynamic programming. The minimum cost to reach step i is the cost of step i plus the minimum of the cost to reach step i-1 and step i-2. Iterate from the third step to the top.",
    implementation: "class Solution:\n    def minCostClimbingStairs(self, cost: List[int]) -> int:\n        cost.append(0)\n        for i in range(len(cost) - 3, -1, -1):\n            cost[i] += min(cost[i + 1], cost[i + 2])\n        return min(cost[0], cost[1])"
  },
  "Target Sum": {
    description: "You are given an integer array nums and an integer target. You want to build an expression out of nums by adding one of the symbols '+' and '-' before each integer in nums and then concatenate all the integers. Return the number of different expressions that you can build, which evaluates to target.",
    explanation: "Use dynamic programming with memoization. The state is defined by the current index and the current sum. For each number, recursively calculate the number of ways by adding and subtracting it. Memoize the results to avoid redundant calculations.",
    implementation: "class Solution:\n    def findTargetSumWays(self, nums: List[int], target: int) -> int:\n        dp = {}\n        def backtrack(i, total):\n            if i == len(nums):\n                return 1 if total == target else 0\n            if (i, total) in dp:\n                return dp[(i, total)]\n            dp[(i, total)] = backtrack(i + 1, total + nums[i]) + backtrack(i + 1, total - nums[i])\n            return dp[(i, total)]\n        return backtrack(0, 0)"
  },
  "Interleaving String": {
    description: "Given strings s1, s2, and s3, find whether s3 is formed by an interleaving of s1 and s2. An interleaving of two strings s and t is a configuration where s and t are divided into n and m substrings respectively, such that s3 is formed by concatenating them in an alternating fashion.",
    explanation: "Use dynamic programming. dp[i][j] is true if s3[:i+j] is an interleaving of s1[:i] and s2[:j]. Initialize dp[0][0] to true. For each cell, check if the current character in s3 matches the corresponding character in s1 or s2 and if the previous state is true.",
    implementation: "class Solution:\n    def isInterleave(self, s1: str, s2: str, s3: str) -> bool:\n        if len(s1) + len(s2) != len(s3): return False\n        dp = [[False] * (len(s2) + 1) for i in range(len(s1) + 1)]\n        dp[len(s1)][len(s2)] = True\n        for i in range(len(s1), -1, -1):\n            for j in range(len(s2), -1, -1):\n                if i < len(s1) and s1[i] == s3[i + j] and dp[i + 1][j]:\n                    dp[i][j] = True\n                if j < len(s2) and s2[j] == s3[i + j] and dp[i][j + 1]:\n                    dp[i][j] = True\n        return dp[0][0]"
  },
  "Longest Palindromic Subsequence": {
    description: "Given a string s, find the longest palindromic subsequence's length in s. A subsequence is a sequence that can be derived from another sequence by deleting some or no elements without changing the order of the remaining elements.",
    explanation: "Use dynamic programming. dp[i][j] is the length of the LPS in s[i:j+1]. If s[i] == s[j], dp[i][j] = 2 + dp[i+1][j-1]. Otherwise, dp[i][j] = max(dp[i+1][j], dp[i][j-1]). Iterate over all possible lengths.",
    implementation: "class Solution:\n    def longestPalindromeSubseq(self, s: str) -> int:\n        dp = [[0] * len(s) for i in range(len(s))]\n        for i in range(len(s) - 1, -1, -1):\n            dp[i][i] = 1\n            for j in range(i + 1, len(s)):\n                if s[i] == s[j]:\n                    dp[i][j] = 2 + dp[i + 1][j - 1]\n                else:\n                    dp[i][j] = max(dp[i + 1][j], dp[i][j - 1])\n        return dp[0][len(s) - 1]"
  },
  "Edit Distance": {
    description: "Given two strings word1 and word2, return the minimum number of operations required to convert word1 to word2. You have the following three operations permitted on a word: Insert a character, Delete a character, Replace a character.",
    explanation: "Use dynamic programming. dp[i][j] is the minimum operations to convert word1[:i] to word2[:j]. If characters match, dp[i][j] = dp[i-1][j-1]. Otherwise, it's 1 + min(insert, delete, replace).",
    implementation: "class Solution:\n    def minDistance(self, word1: str, word2: str) -> int:\n        dp = [[float('inf')] * (len(word2) + 1) for i in range(len(word1) + 1)]\n        for j in range(len(word2) + 1):\n            dp[len(word1)][j] = len(word2) - j\n        for i in range(len(word1) + 1):\n            dp[i][len(word2)] = len(word1) - i\n        for i in range(len(word1) - 1, -1, -1):\n            for j in range(len(word2) - 1, -1, -1):\n                if word1[i] == word2[j]:\n                    dp[i][j] = dp[i + 1][j + 1]\n                else:\n                    dp[i][j] = 1 + min(dp[i + 1][j], dp[i][j + 1], dp[i + 1][j + 1])\n        return dp[0][0]"
  },
  "Burst Balloons": {
    description: "You are given n balloons, indexed from 0 to n - 1. Each balloon is painted with a number on it represented by an array nums. You are asked to burst all the balloons. If you burst the ith balloon, you will get nums[i - 1] * nums[i] * nums[i + 1] coins. Return the maximum coins you can collect by bursting the balloons wisely.",
    explanation: "Use dynamic programming with memoization. Add 1 to the beginning and end of nums. dp[l][r] is the max coins obtained by bursting balloons between l and r. Iterate over all possible last balloons to burst in the range, and recursively calculate the max coins.",
    implementation: "class Solution:\n    def maxCoins(self, nums: List[int]) -> int:\n        nums = [1] + nums + [1]\n        dp = {}\n        def dfs(l, r):\n            if l > r: return 0\n            if (l, r) in dp: return dp[(l, r)]\n            dp[(l, r)] = 0\n            for i in range(l, r + 1):\n                coins = nums[l - 1] * nums[i] * nums[r + 1]\n                coins += dfs(l, i - 1) + dfs(i + 1, r)\n                dp[(l, r)] = max(dp[(l, r)], coins)\n            return dp[(l, r)]\n        return dfs(1, len(nums) - 2)"
  },
  "Regular Expression Matching": {
    description: "Given an input string s and a pattern p, implement regular expression matching with support for '.' and '*' where: '.' Matches any single character. '*' Matches zero or more of the preceding element.",
    explanation: "Use dynamic programming with memoization. dp[i][j] is true if s[i:] matches p[j:]. Check if the current characters match. If the next character in p is '*', you can either use it (if current characters match) or skip it. Otherwise, advance both pointers if characters match.",
    implementation: "class Solution:\n    def isMatch(self, s: str, p: str) -> bool:\n        cache = {}\n        def dfs(i, j):\n            if (i, j) in cache: return cache[(i, j)]\n            if i >= len(s) and j >= len(p): return True\n            if j >= len(p): return False\n            match = i < len(s) and (s[i] == p[j] or p[j] == '.')\n            if (j + 1) < len(p) and p[j + 1] == '*':\n                cache[(i, j)] = (dfs(i, j + 2) or (match and dfs(i + 1, j)))\n                return cache[(i, j)]\n            if match:\n                cache[(i, j)] = dfs(i + 1, j + 1)\n                return cache[(i, j)]\n            cache[(i, j)] = False\n            return False\n        return dfs(0, 0)"
  },
  "Maximum Alternating Subsequence Sum": {
    description: "The alternating sum of a 0-indexed array is defined as the sum of the elements at even indices minus the sum of the elements at odd indices. Given an array nums, return the maximum alternating sum of any subsequence of nums.",
    explanation: "Use dynamic programming. Maintain two states: the maximum sum ending with an even length subsequence (sumEven) and the maximum sum ending with an odd length subsequence (sumOdd). Update them at each step.",
    implementation: "class Solution:\n    def maxAlternatingSum(self, nums: List[int]) -> int:\n        sumEven, sumOdd = 0, 0\n        for i in range(len(nums) - 1, -1, -1):\n            tmpEven = max(sumOdd + nums[i], sumEven)\n            tmpOdd = max(sumEven - nums[i], sumOdd)\n            sumEven, sumOdd = tmpEven, tmpOdd\n        return sumEven"
  },
  "Partition Equal Subset Sum": {
    description: "Given an integer array nums, return true if you can partition the array into two subsets such that the sum of the elements in both subsets is equal or false otherwise.",
    explanation: "Use dynamic programming. The target sum is half of the total sum. Use a set to keep track of all possible sums that can be formed by the elements seen so far. If the target sum is in the set, return true.",
    implementation: "class Solution:\n    def canPartition(self, nums: List[int]) -> bool:\n        if sum(nums) % 2: return False\n        dp = set()\n        dp.add(0)\n        target = sum(nums) // 2\n        for i in range(len(nums) - 1, -1, -1):\n            nextDP = set()\n            for t in dp:\n                if (t + nums[i]) == target: return True\n                nextDP.add(t + nums[i])\n                nextDP.add(t)\n            dp = nextDP\n        return True"
  },
  "Count Vowels Permutation": {
    description: "Given an integer n, your task is to count how many strings of length n can be formed under the following rules: Each character is a lower case vowel ('a', 'e', 'i', 'o', 'u'). Each vowel has specific rules for what can follow it.",
    explanation: "Use dynamic programming. Maintain the count of strings ending in each vowel for length i. Update the counts for length i+1 based on the rules. For example, 'a' can only be followed by 'e', so the new count for 'e' includes the previous count for 'a'.",
    implementation: "class Solution:\n    def countVowelPermutation(self, n: int) -> int:\n        dp = [[], [1, 1, 1, 1, 1]]\n        a, e, i, o, u = 0, 1, 2, 3, 4\n        mod = 10**9 + 7\n        for j in range(2, n + 1):\n            dp.append([0, 0, 0, 0, 0])\n            dp[j][a] = (dp[j - 1][e] + dp[j - 1][i] + dp[j - 1][u]) % mod\n            dp[j][e] = (dp[j - 1][a] + dp[j - 1][i]) % mod\n            dp[j][i] = (dp[j - 1][e] + dp[j - 1][o]) % mod\n            dp[j][o] = (dp[j - 1][i]) % mod\n            dp[j][u] = (dp[j - 1][i] + dp[j - 1][o]) % mod\n        return sum(dp[n]) % mod"
  },
  "Gas Station": {
    description: "There are n gas stations along a circular route, where the amount of gas at the ith station is gas[i]. You have a car with an unlimited gas tank and it costs cost[i] of gas to travel from the ith station to its next (i + 1)th station. You begin the journey with an empty tank at one of the gas stations. Given two integer arrays gas and cost, return the starting gas station's index if you can travel around the circuit once in the clockwise direction, otherwise return -1.",
    explanation: "Greedy approach. If the total gas is less than the total cost, it's impossible. Otherwise, there is a unique solution. Keep a running total of the gas minus cost. If it drops below 0, the starting station cannot be any station visited so far. Reset the running total and set the next station as the potential start.",
    implementation: "class Solution:\n    def canCompleteCircuit(self, gas: List[int], cost: List[int]) -> int:\n        if sum(gas) < sum(cost): return -1\n        total = 0\n        res = 0\n        for i in range(len(gas)):\n            total += (gas[i] - cost[i])\n            if total < 0:\n                total = 0\n                res = i + 1\n        return res"
  },
  "Hand of Straights": {
    description: "Alice has some number of cards and she wants to rearrange the cards into groups so that each group is of size groupSize, and consists of groupSize consecutive cards. Given an integer array hand where hand[i] is the value written on the ith card and an integer groupSize, return true if she can rearrange the cards, or false otherwise.",
    explanation: "Use a hash map to count the frequencies of each card. Use a min-heap to keep track of the available cards in sorted order. Repeatedly pop the smallest card and try to form a group of consecutive cards. Decrement the counts and remove cards from the heap when their count reaches 0.",
    implementation: "class Solution:\n    def isNStraightHand(self, hand: List[int], groupSize: int) -> bool:\n        if len(hand) % groupSize: return False\n        import collections, heapq\n        count = collections.Counter(hand)\n        minH = list(count.keys())\n        heapq.heapify(minH)\n        while minH:\n            first = minH[0]\n            for i in range(first, first + groupSize):\n                if i not in count: return False\n                count[i] -= 1\n                if count[i] == 0:\n                    if i != minH[0]: return False\n                    heapq.heappop(minH)\n        return True"
  },
  "Merge Triplets to Form Target Triplet": {
    description: "A triplet is an array of three integers. You are given a 2D integer array triplets, where triplets[i] = [ai, bi, ci] describes the ith triplet. You are also given an integer array target = [x, y, z] that describes the triplet you want to obtain. To obtain target, you may apply the following operation on triplets any number of times: Choose two indices (0-indexed) i and j (i != j) and update triplets[j] to become [max(ai, aj), max(bi, bj), max(ci, cj)]. Return true if it is possible to obtain the target triplet [x, y, z] as an element of triplets, or false otherwise.",
    explanation: "Greedy approach. Iterate through the triplets. If any element in a triplet is greater than the corresponding element in the target, we cannot use this triplet. Otherwise, we can use it to potentially build the target. Keep track of the maximum values seen so far for each position. If the maximums match the target, return true.",
    implementation: "class Solution:\n    def mergeTriplets(self, triplets: List[List[int]], target: List[int]) -> bool:\n        good = set()\n        for t in triplets:\n            if t[0] > target[0] or t[1] > target[1] or t[2] > target[2]:\n                continue\n            for i, v in enumerate(t):\n                if v == target[i]:\n                    good.add(i)\n        return len(good) == 3"
  },
  "Partition Labels": {
    description: "You are given a string s. We want to partition the string into as many parts as possible so that each letter appears in at most one part. Note that the partition is done so that after concatenating all the parts in order, the resultant string should be s. Return a list of integers representing the size of these parts.",
    explanation: "Greedy approach. First, find the last occurrence index of each character in the string. Then, iterate through the string, keeping track of the maximum last occurrence index seen so far in the current partition. When the current index reaches this maximum, it means all characters in the current partition only appear within it. Add the size to the result and start a new partition.",
    implementation: "class Solution:\n    def partitionLabels(self, s: str) -> List[int]:\n        lastIndex = {}\n        for i, c in enumerate(s):\n            lastIndex[c] = i\n        res = []\n        size, end = 0, 0\n        for i, c in enumerate(s):\n            size += 1\n            end = max(end, lastIndex[c])\n            if i == end:\n                res.append(size)\n                size = 0\n        return res"
  },
  "Valid Parenthesis String": {
    description: "Given a string s containing only three types of characters: '(', ')' and '*', return true if s is valid. '*' can be treated as a single right parenthesis ')', a single left parenthesis '(', or an empty string.",
    explanation: "Greedy approach. Keep track of the minimum and maximum possible number of open parentheses. When encountering '(', increment both. When encountering ')', decrement both. When encountering '*', decrement min and increment max. If max becomes negative, it's invalid. If min becomes negative, reset it to 0. At the end, min must be 0.",
    implementation: "class Solution:\n    def checkValidString(self, s: str) -> bool:\n        leftMin, leftMax = 0, 0\n        for c in s:\n            if c == '(':\n                leftMin, leftMax = leftMin + 1, leftMax + 1\n            elif c == ')':\n                leftMin, leftMax = leftMin - 1, leftMax - 1\n            else:\n                leftMin, leftMax = leftMin - 1, leftMax + 1\n            if leftMax < 0: return False\n            if leftMin < 0: leftMin = 0\n        return leftMin == 0"
  },
  "Jump Game II": {
    description: "You are given a 0-indexed array of integers nums of length n. You are initially positioned at nums[0]. Each element nums[i] represents the maximum length of a forward jump from index i. In other words, if you are at nums[i], you can jump to any nums[i + j] where: 0 <= j <= nums[i] and i + j < n. Return the minimum number of jumps to reach nums[n - 1].",
    explanation: "Greedy approach (BFS-like). Maintain a window [l, r] of reachable indices with the current number of jumps. In each step, find the farthest reachable index from any index in the current window. Update the window to [r + 1, farthest] and increment the jump count.",
    implementation: "class Solution:\n    def jump(self, nums: List[int]) -> int:\n        res = 0\n        l = r = 0\n        while r < len(nums) - 1:\n            farthest = 0\n            for i in range(l, r + 1):\n                farthest = max(farthest, i + nums[i])\n            l = r + 1\n            r = farthest\n            res += 1\n        return res"
  },
  "Maximum Points You Can Obtain from Cards": {
    description: "There are several cards arranged in a row, and each card has an associated number of points. The points are given in the integer array cardPoints. In one step, you can take one card from the beginning or from the end of the row. You have to take exactly k cards. Your score is the sum of the points of the cards you have taken. Given the integer array cardPoints and the integer k, return the maximum score you can obtain.",
    explanation: "Sliding window. Instead of finding the maximum sum of k cards from the ends, find the minimum sum of a contiguous subarray of length n - k. The maximum score is the total sum minus this minimum subarray sum.",
    implementation: "class Solution:\n    def maxScore(self, cardPoints: List[int], k: int) -> int:\n        l, r = 0, len(cardPoints) - k\n        total = sum(cardPoints[r:])\n        res = total\n        while r < len(cardPoints):\n            total += (cardPoints[l] - cardPoints[r])\n            res = max(res, total)\n            l += 1\n            r += 1\n        return res"
  },
  "Longest Subarray of 1's After Deleting One Element": {
    description: "Given a binary array nums, you should delete one element from it. Return the size of the longest non-empty subarray containing only 1's in the resulting array. Return 0 if there is no such subarray.",
    explanation: "Sliding window. Maintain a window with at most one 0. If the number of 0s exceeds 1, shrink the window from the left. The size of the longest subarray is the maximum window size minus 1.",
    implementation: "class Solution:\n    def longestSubarray(self, nums: List[int]) -> int:\n        zeroCount = 0\n        longestWindow = 0\n        start = 0\n        for i in range(len(nums)):\n            zeroCount += (nums[i] == 0)\n            while zeroCount > 1:\n                zeroCount -= (nums[start] == 0)\n                start += 1\n            longestWindow = max(longestWindow, i - start)\n        return longestWindow"
  },
  "Fruit Into Baskets": {
    description: "You are visiting a farm that has a single row of fruit trees arranged from left to right. The trees are represented by an integer array fruits where fruits[i] is the type of fruit the ith tree produces. You want to collect as much fruit as possible. However, the owner has some strict rules that you must follow: You only have two baskets, and each basket can only hold a single type of fruit. There is no limit on the amount of fruit each basket can hold. Starting from any tree of your choice, you must pick exactly one fruit from every tree (including the start tree) while moving to the right. The picked fruits must fit in one of your baskets. Once you reach a tree with fruit that cannot fit in your baskets, you must stop. Given the integer array fruits, return the maximum number of fruits you can pick.",
    explanation: "Sliding window. Maintain a window with at most two types of fruits. Use a hash map to keep track of the count of each fruit type in the window. If the number of types exceeds 2, shrink the window from the left until there are only 2 types. The maximum number of fruits is the maximum window size.",
    implementation: "class Solution:\n    def totalFruit(self, fruits: List[int]) -> int:\n        import collections\n        count = collections.defaultdict(int)\n        l, total, res = 0, 0, 0\n        for r in range(len(fruits)):\n            count[fruits[r]] += 1\n            total += 1\n            while len(count) > 2:\n                f = fruits[l]\n                count[f] -= 1\n                total -= 1\n                l += 1\n                if not count[f]:\n                    count.pop(f)\n            res = max(res, total)\n        return res"
  },
  "Find All Anagrams in a String": {
    description: "Given two strings s and p, return an array of all the start indices of p's anagrams in s. You may return the answer in any order. An Anagram is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.",
    explanation: "Sliding window with hash maps. Maintain a frequency map for string p and a frequency map for the current window in s. Slide the window of size len(p) over s. If the frequency maps match, add the start index to the result.",
    implementation: "class Solution:\n    def findAnagrams(self, s: str, p: str) -> List[int]:\n        if len(p) > len(s): return []\n        pCount, sCount = {}, {}\n        for i in range(len(p)):\n            pCount[p[i]] = 1 + pCount.get(p[i], 0)\n            sCount[s[i]] = 1 + sCount.get(s[i], 0)\n        res = [0] if sCount == pCount else []\n        l = 0\n        for r in range(len(p), len(s)):\n            sCount[s[r]] = 1 + sCount.get(s[r], 0)\n            sCount[s[l]] -= 1\n            if sCount[s[l]] == 0:\n                sCount.pop(s[l])\n            l += 1\n            if sCount == pCount:\n                res.append(l)\n        return res"
  },
  "Maximum Erasure Value": {
    description: "You are given an array of positive integers nums and want to erase a subarray containing unique elements. The score you get by erasing the subarray is equal to the sum of its elements. Return the maximum score you can get by erasing exactly one subarray.",
    explanation: "Sliding window with a set. Maintain a window of unique elements. If a duplicate is encountered, shrink the window from the left until the duplicate is removed. Keep track of the sum of elements in the window and update the maximum score.",
    implementation: "class Solution:\n    def maximumUniqueSubarray(self, nums: List[int]) -> int:\n        seen = set()\n        current_sum = 0\n        max_sum = 0\n        l = 0\n        for r in range(len(nums)):\n            while nums[r] in seen:\n                seen.remove(nums[l])\n                current_sum -= nums[l]\n                l += 1\n            seen.add(nums[r])\n            current_sum += nums[r]\n            max_sum = max(max_sum, current_sum)\n        return max_sum"
  },
  "Binary Tree Right Side View": {
    description: "Given the root of a binary tree, imagine yourself standing on the right side of it, return the values of the nodes you can see ordered from top to bottom.",
    explanation: "Use BFS (Level Order Traversal). For each level, the last node processed is the one visible from the right side. Add its value to the result.",
    implementation: "class Solution:\n    def rightSideView(self, root: Optional[TreeNode]) -> List[int]:\n        import collections\n        res = []\n        q = collections.deque([root])\n        while q:\n            rightSide = None\n            qLen = len(q)\n            for i in range(qLen):\n                node = q.popleft()\n                if node:\n                    rightSide = node\n                    q.append(node.left)\n                    q.append(node.right)\n            if rightSide:\n                res.append(rightSide.val)\n        return res"
  },
  "Count Good Nodes in Binary Tree": {
    description: "Given a binary tree root, a node X in the tree is named good if in the path from root to X there are no nodes with a value greater than X. Return the number of good nodes in the binary tree.",
    explanation: "Use DFS. Pass the maximum value seen so far along the path. If the current node's value is greater than or equal to the maximum, it's a good node. Update the maximum and recursively check the left and right children.",
    implementation: "class Solution:\n    def goodNodes(self, root: TreeNode) -> int:\n        def dfs(node, maxVal):\n            if not node:\n                return 0\n            res = 1 if node.val >= maxVal else 0\n            maxVal = max(maxVal, node.val)\n            res += dfs(node.left, maxVal)\n            res += dfs(node.right, maxVal)\n            return res\n        return dfs(root, root.val)"
  },
  "Lowest Common Ancestor of a Binary Tree": {
    description: "Given a binary tree, find the lowest common ancestor (LCA) of two given nodes in the tree. According to the definition of LCA on Wikipedia: “The lowest common ancestor is defined between two nodes p and q as the lowest node in T that has both p and q as descendants (where we allow a node to be a descendant of itself).”",
    explanation: "Use DFS. If the current node is null, p, or q, return the current node. Recursively search the left and right subtrees. If both subtrees return a non-null node, the current node is the LCA. Otherwise, return the non-null node from the subtrees.",
    implementation: "class Solution:\n    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':\n        if not root or root == p or root == q:\n            return root\n        l = self.lowestCommonAncestor(root.left, p, q)\n        r = self.lowestCommonAncestor(root.right, p, q)\n        if l and r:\n            return root\n        return l or r"
  },
  "Diameter of Binary Tree": {
    description: "Given the root of a binary tree, return the length of the diameter of the tree. The diameter of a binary tree is the length of the longest path between any two nodes in a tree. This path may or may not pass through the root. The length of a path between two nodes is represented by the number of edges between them.",
    explanation: "Use DFS. For each node, calculate the height of its left and right subtrees. The diameter passing through the current node is the sum of these heights. Update the maximum diameter seen so far. Return the height of the current node (1 + max(left_height, right_height)).",
    implementation: "class Solution:\n    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:\n        res = [0]\n        def dfs(root):\n            if not root:\n                return -1\n            left = dfs(root.left)\n            right = dfs(root.right)\n            res[0] = max(res[0], 2 + left + right)\n            return 1 + max(left, right)\n        dfs(root)\n        return res[0]"
  },
  "Balanced Binary Tree": {
    description: "Given a binary tree, determine if it is height-balanced. A height-balanced binary tree is a binary tree in which the depth of the two subtrees of every node never differs by more than one.",
    explanation: "Use DFS. For each node, calculate the height of its left and right subtrees. If the difference in heights is greater than 1, or if either subtree is unbalanced, the tree is unbalanced. Return the height and a boolean indicating balance.",
    implementation: "class Solution:\n    def isBalanced(self, root: Optional[TreeNode]) -> bool:\n        def dfs(root):\n            if not root: return [True, 0]\n            left, right = dfs(root.left), dfs(root.right)\n            balanced = (left[0] and right[0] and abs(left[1] - right[1]) <= 1)\n            return [balanced, 1 + max(left[1], right[1])]\n        return dfs(root)[0]"
  },
  "Copy List with Random Pointer": {
    description: "A linked list of length n is given such that each node contains an additional random pointer, which could point to any node in the list, or null. Construct a deep copy of the list. The deep copy should consist of exactly n brand new nodes, where each new node has its value set to the value of its corresponding original node. Both the next and random pointer of the new nodes should point to new nodes in the copied list such that the pointers in the original list and copied list represent the same list state. None of the pointers in the new list should point to nodes in the original list.",
    explanation: "Use a hash map to map original nodes to their copies. Iterate through the original list twice. In the first pass, create the copies and populate the hash map. In the second pass, set the next and random pointers of the copies using the hash map.",
    implementation: "class Solution:\n    def copyRandomList(self, head: 'Optional[Node]') -> 'Optional[Node]':\n        oldToCopy = {None: None}\n        cur = head\n        while cur:\n            copy = Node(cur.val)\n            oldToCopy[cur] = copy\n            cur = cur.next\n        cur = head\n        while cur:\n            copy = oldToCopy[cur]\n            copy.next = oldToCopy[cur.next]\n            copy.random = oldToCopy[cur.random]\n            cur = cur.next\n        return oldToCopy[head]"
  },
  "Add Two Numbers": {
    description: "You are given two non-empty linked lists representing two non-negative integers. The digits are stored in reverse order, and each of their nodes contains a single digit. Add the two numbers and return the sum as a linked list. You may assume the two numbers do not contain any leading zero, except the number 0 itself.",
    explanation: "Iterate through both lists simultaneously. Add the values of the current nodes and the carry from the previous addition. Create a new node with the sum modulo 10 and update the carry. Continue until both lists and the carry are exhausted.",
    implementation: "class Solution:\n    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:\n        dummy = ListNode()\n        cur = dummy\n        carry = 0\n        while l1 or l2 or carry:\n            v1 = l1.val if l1 else 0\n            v2 = l2.val if l2 else 0\n            val = v1 + v2 + carry\n            carry = val // 10\n            val = val % 10\n            cur.next = ListNode(val)\n            cur = cur.next\n            l1 = l1.next if l1 else None\n            l2 = l2.next if l2 else None\n        return dummy.next"
  },
  "Find the Duplicate Number": {
    description: "Given an array of integers nums containing n + 1 integers where each integer is in the range [1, n] inclusive. There is only one repeated number in nums, return this repeated number. You must solve the problem without modifying the array nums and uses only constant extra space.",
    explanation: "Use Floyd's Tortoise and Hare (Cycle Detection) algorithm. Treat the array as a linked list where the value at an index is the pointer to the next index. Find the intersection point of the slow and fast pointers. Then, reset the slow pointer to the start and move both pointers at the same speed to find the start of the cycle, which is the duplicate number.",
    implementation: "class Solution:\n    def findDuplicate(self, nums: List[int]) -> int:\n        slow, fast = 0, 0\n        while True:\n            slow = nums[slow]\n            fast = nums[nums[fast]]\n            if slow == fast:\n                break\n        slow2 = 0\n        while True:\n            slow = nums[slow]\n            slow2 = nums[slow2]\n            if slow == slow2:\n                return slow"
  },
  "LRU Cache": {
    description: "Design a data structure that follows the constraints of a Least Recently Used (LRU) cache. Implement the LRUCache class: LRUCache(int capacity) Initialize the LRU cache with positive size capacity. int get(int key) Return the value of the key if the key exists, otherwise return -1. void put(int key, int value) Update the value of the key if the key exists. Otherwise, add the key-value pair to the cache. If the number of keys exceeds the capacity from this operation, evict the least recently used key. The functions get and put must each run in O(1) average time complexity.",
    explanation: "Use a doubly linked list and a hash map. The hash map stores keys and pointers to the corresponding nodes in the linked list. The linked list maintains the order of usage, with the most recently used at the head and the least recently used at the tail. Update the linked list on every get and put operation.",
    implementation: "class Node:\n    def __init__(self, key, val):\n        self.key, self.val = key, val\n        self.prev = self.next = None\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        self.cap = capacity\n        self.cache = {}\n        self.left, self.right = Node(0, 0), Node(0, 0)\n        self.left.next, self.right.prev = self.right, self.left\n    def remove(self, node):\n        prev, nxt = node.prev, node.next\n        prev.next, nxt.prev = nxt, prev\n    def insert(self, node):\n        prev, nxt = self.right.prev, self.right\n        prev.next = nxt.prev = node\n        node.prev, node.next = prev, nxt\n    def get(self, key: int) -> int:\n        if key in self.cache:\n            self.remove(self.cache[key])\n            self.insert(self.cache[key])\n            return self.cache[key].val\n        return -1\n    def put(self, key: int, value: int) -> None:\n        if key in self.cache:\n            self.remove(self.cache[key])\n        self.cache[key] = Node(key, value)\n        self.insert(self.cache[key])\n        if len(self.cache) > self.cap:\n            lru = self.left.next\n            self.remove(lru)\n            del self.cache[lru.key]"
  },
  "Reverse Nodes in k-Group": {
    description: "Given the head of a linked list, reverse the nodes of the list k at a time, and return the modified list. k is a positive integer and is less than or equal to the length of the linked list. If the number of nodes is not a multiple of k then left-out nodes, in the end, should remain as it is. You may not alter the values in the list's nodes, only nodes themselves may be changed.",
    explanation: "Iterate through the list to find the kth node. If it exists, reverse the k nodes. Keep track of the previous group's tail to connect it to the new head of the reversed group. Recursively or iteratively process the remaining groups.",
    implementation: "class Solution:\n    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:\n        dummy = ListNode(0, head)\n        groupPrev = dummy\n        while True:\n            kth = self.getKth(groupPrev, k)\n            if not kth:\n                break\n            groupNext = kth.next\n            prev, curr = kth.next, groupPrev.next\n            while curr != groupNext:\n                tmp = curr.next\n                curr.next = prev\n                prev = curr\n                curr = tmp\n            tmp = groupPrev.next\n            groupPrev.next = kth\n            groupPrev = tmp\n        return dummy.next\n    def getKth(self, curr, k):\n        while curr and k > 0:\n            curr = curr.next\n            k -= 1\n        return curr"
  },
  "Coin Change II": {
    description: "You are given an integer array coins representing coins of different denominations and an integer amount representing a total amount of money. Return the number of combinations that make up that amount. If that amount of money cannot be made up by any combination of the coins, return 0.",
    explanation: "Use dynamic programming. dp[i] is the number of combinations to make up amount i. Initialize dp[0] to 1. For each coin, iterate from the coin's value to the target amount, adding the number of combinations for the remaining amount.",
    implementation: "class Solution:\n    def change(self, amount: int, coins: List[int]) -> int:\n        dp = [0] * (amount + 1)\n        dp[0] = 1\n        for i in range(len(coins) - 1, -1, -1):\n            nextDP = [0] * (amount + 1)\n            nextDP[0] = 1\n            for a in range(1, amount + 1):\n                nextDP[a] = dp[a]\n                if a - coins[i] >= 0:\n                    nextDP[a] += nextDP[a - coins[i]]\n            dp = nextDP\n        return dp[amount]"
  },
  "Best Time to Buy and Sell Stock with Cooldown": {
    description: "You are given an array prices where prices[i] is the price of a given stock on the ith day. Find the maximum profit you can achieve. You may complete as many transactions as you like (i.e., buy one and sell one share of the stock multiple times) with the following restrictions: After you sell your stock, you cannot buy stock on the next day (i.e., cooldown one day).",
    explanation: "Use dynamic programming with state machine. Three states: held (holding a stock), sold (just sold a stock, entering cooldown), and reset (not holding, not in cooldown). Update the states based on the previous states and the current price.",
    implementation: "class Solution:\n    def maxProfit(self, prices: List[int]) -> int:\n        dp = {}\n        def dfs(i, buying):\n            if i >= len(prices): return 0\n            if (i, buying) in dp: return dp[(i, buying)]\n            cooldown = dfs(i + 1, buying)\n            if buying:\n                buy = dfs(i + 1, not buying) - prices[i]\n                dp[(i, buying)] = max(buy, cooldown)\n            else:\n                sell = dfs(i + 2, not buying) + prices[i]\n                dp[(i, buying)] = max(sell, cooldown)\n            return dp[(i, buying)]\n        return dfs(0, True)"
  },
  "Sort Colors": {
    description: "Given an array nums with n objects colored red, white, or blue, sort them in-place so that objects of the same color are adjacent, with the colors in the order red, white, and blue. We will use the integers 0, 1, and 2 to represent the color red, white, and blue, respectively.",
    explanation: "Use the Dutch National Flag algorithm. Maintain three pointers: low for 0s, mid for 1s, and high for 2s. Iterate through the array with mid, swapping elements to their correct regions based on their value.",
    implementation: "class Solution:\n    def sortColors(self, nums: List[int]) -> None:\n        low, mid, high = 0, 0, len(nums) - 1\n        while mid <= high:\n            if nums[mid] == 0:\n                nums[low], nums[mid] = nums[mid], nums[low]\n                low += 1\n                mid += 1\n            elif nums[mid] == 1:\n                mid += 1\n            else:\n                nums[mid], nums[high] = nums[high], nums[mid]\n                high -= 1"
  },
  "Merge Sorted Array": {
    description: "You are given two integer arrays nums1 and nums2, sorted in non-decreasing order, and two integers m and n, representing the number of elements in nums1 and nums2 respectively. Merge nums1 and nums2 into a single array sorted in non-decreasing order.",
    explanation: "Start merging from the end of both arrays. Maintain three pointers: p1 for the end of the valid elements in nums1, p2 for the end of nums2, and p for the end of the merged array in nums1. Compare elements at p1 and p2, placing the larger one at p.",
    implementation: "class Solution:\n    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:\n        p1, p2, p = m - 1, n - 1, m + n - 1\n        while p1 >= 0 and p2 >= 0:\n            if nums1[p1] > nums2[p2]:\n                nums1[p] = nums1[p1]\n                p1 -= 1\n            else:\n                nums1[p] = nums2[p2]\n                p2 -= 1\n            p -= 1\n        nums1[:p2 + 1] = nums2[:p2 + 1]"
  },
  "Squares of a Sorted Array": {
    description: "Given an integer array nums sorted in non-decreasing order, return an array of the squares of each number sorted in non-decreasing order.",
    explanation: "Use two pointers, one at the beginning and one at the end of the array. Compare the absolute values of the elements at the pointers. Square the larger absolute value and place it at the end of the result array, moving the corresponding pointer inward.",
    implementation: "class Solution:\n    def sortedSquares(self, nums: List[int]) -> List[int]:\n        n = len(nums)\n        res = [0] * n\n        left, right = 0, n - 1\n        for i in range(n - 1, -1, -1):\n            if abs(nums[left]) > abs(nums[right]):\n                res[i] = nums[left] ** 2\n                left += 1\n            else:\n                res[i] = nums[right] ** 2\n                right -= 1\n        return res"
  },
  "Intersection of Two Arrays": {
    description: "Given two integer arrays nums1 and nums2, return an array of their intersection. Each element in the result must be unique and you may return the result in any order.",
    explanation: "Convert both arrays to sets to remove duplicates and allow for O(1) lookups. Then, find the intersection of the two sets.",
    implementation: "class Solution:\n    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:\n        set1 = set(nums1)\n        set2 = set(nums2)\n        return list(set1.intersection(set2))"
  },
  "Intersection of Two Arrays II": {
    description: "Given two integer arrays nums1 and nums2, return an array of their intersection. Each element in the result must appear as many times as it shows in both arrays and you may return the result in any order.",
    explanation: "Use a hash map to count the frequencies of elements in the first array. Then, iterate through the second array. If an element is in the hash map and its count is greater than 0, add it to the result and decrement its count.",
    implementation: "from collections import Counter\nclass Solution:\n    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:\n        counts = Counter(nums1)\n        res = []\n        for num in nums2:\n            if counts[num] > 0:\n                res.append(num)\n                counts[num] -= 1\n        return res"
  },
  "Sort an Array": {
    description: "Given an array of integers nums, sort the array in ascending order and return it. You must solve the problem without using any built-in functions in O(nlog(n)) time complexity and with the smallest space complexity possible.",
    explanation: "We can use Merge Sort to achieve O(n log n) time complexity. Merge Sort divides the array into halves, sorts them recursively, and merges the sorted halves.",
    implementation: "class Solution:\n    def sortArray(self, nums: List[int]) -> List[int]:\n        # Base case: if the array has 1 or 0 elements, it's already sorted\n        if len(nums) <= 1:\n            return nums\n        \n        # Find the middle point to divide the array into two halves\n        mid = len(nums) // 2\n        left_half = self.sortArray(nums[:mid])\n        right_half = self.sortArray(nums[mid:])\n        \n        # Merge the sorted halves\n        return self.merge(left_half, right_half)\n        \n    def merge(self, left: List[int], right: List[int]) -> List[int]:\n        sorted_arr = []\n        i = j = 0\n        \n        # Compare elements from both halves and add the smaller one to the result\n        while i < len(left) and j < len(right):\n            if left[i] < right[j]:\n                sorted_arr.append(left[i])\n                i += 1\n            else:\n                sorted_arr.append(right[j])\n                j += 1\n                \n        # Add any remaining elements from the left or right half\n        sorted_arr.extend(left[i:])\n        sorted_arr.extend(right[j:])\n        \n        return sorted_arr"
  },
  "Largest Number": {
    description: "Given a list of non-negative integers nums, arrange them such that they form the largest number and return it. Since the result may be very large, so you need to return a string instead of an integer.",
    explanation: "Custom sort the strings. For two numbers a and b, compare a+b and b+a. If a+b > b+a, then a should come before b.",
    implementation: "class Solution:\n    def largestNumber(self, nums: List[int]) -> str:\n        # Convert all integers to strings for concatenation comparison\n        nums_str = [str(num) for num in nums]\n        \n        # Define a custom sorting key\n        # We compare two strings by concatenating them in both possible orders\n        from functools import cmp_to_key\n        def compare(n1, n2):\n            if n1 + n2 > n2 + n1:\n                return -1 # n1 should come first\n            elif n1 + n2 < n2 + n1:\n                return 1  # n2 should come first\n            else:\n                return 0\n                \n        # Sort the array using the custom comparison function\n        nums_str.sort(key=cmp_to_key(compare))\n        \n        # Join the sorted strings to form the largest number\n        largest_num = \"\".join(nums_str)\n        \n        # Handle the edge case where the array contains only zeros (e.g., [0, 0])\n        return \"0\" if largest_num[0] == \"0\" else largest_num"
  },
  "Sort List": {
    description: "Given the head of a linked list, return the list after sorting it in ascending order.",
    explanation: "Merge sort is ideal for linked lists because it requires O(1) auxiliary space (bottom-up) or O(log n) stack space (top-down) and O(n log n) time. We split the list using the slow/fast pointer technique, sort each half, and merge them.",
    implementation: "class Solution:\n    def sortList(self, head: Optional[ListNode]) -> Optional[ListNode]:\n        # Base case: if the list is empty or has only one node, it's already sorted\n        if not head or not head.next:\n            return head\n            \n        # Step 1: Split the list into two halves using slow and fast pointers\n        left = head\n        right = self.getMid(head)\n        tmp = right.next\n        right.next = None # Break the link\n        right = tmp\n        \n        # Step 2: Recursively sort both halves\n        left = self.sortList(left)\n        right = self.sortList(right)\n        \n        # Step 3: Merge the sorted halves\n        return self.merge(left, right)\n        \n    def getMid(self, head):\n        slow, fast = head, head.next\n        # Fast moves 2 steps, slow moves 1 step. When fast reaches the end, slow is at the middle.\n        while fast and fast.next:\n            slow = slow.next\n            fast = fast.next.next\n        return slow\n        \n    def merge(self, list1, list2):\n        # Dummy node to simplify edge cases when merging\n        dummy = ListNode()\n        tail = dummy\n        \n        # Traverse both lists and append the smaller value to the merged list\n        while list1 and list2:\n            if list1.val < list2.val:\n                tail.next = list1\n                list1 = list1.next\n            else:\n                tail.next = list2\n                list2 = list2.next\n            tail = tail.next\n            \n        # Append any remaining nodes\n        if list1:\n            tail.next = list1\n        if list2:\n            tail.next = list2\n            \n        return dummy.next"
  },
  "Insertion Sort List": {
    description: "Given the head of a singly linked list, sort the list using insertion sort, and return the sorted list's head.",
    explanation: "Create a dummy node to hold the sorted portion. Iterate through the original list, and for each node, find its correct position in the sorted portion and insert it.",
    implementation: "class Solution:\n    def insertionSortList(self, head: Optional[ListNode]) -> Optional[ListNode]:\n        # Dummy node acts as the head of the sorted list\n        dummy = ListNode(0)\n        curr = head\n        \n        while curr:\n            # At each iteration, we insert 'curr' into the sorted list\n            prev = dummy\n            \n            # Find the correct position to insert 'curr'\n            # We traverse the sorted list until we find a node strictly greater than 'curr'\n            while prev.next and prev.next.val <= curr.val:\n                prev = prev.next\n                \n            # Store the next node to process\n            next_node = curr.next\n            \n            # Insert 'curr' between 'prev' and 'prev.next'\n            curr.next = prev.next\n            prev.next = curr\n            \n            # Move to the next node in the original list\n            curr = next_node\n            \n        return dummy.next"
  },
  "Sort Characters By Frequency": {
    description: "Given a string s, sort it in decreasing order based on the frequency of the characters. The frequency of a character is the number of times it appears in the string.",
    explanation: "Count the frequencies of each character using a hash map. Then, sort the characters based on their frequencies in descending order and build the resulting string.",
    implementation: "class Solution:\n    def frequencySort(self, s: str) -> str:\n        # Step 1: Count the frequency of each character\n        from collections import Counter\n        count = Counter(s)\n        \n        # Step 2: Sort the characters based on their frequency in descending order\n        # count.keys() gives the unique characters\n        # key=lambda x: count[x] sorts them by their frequency\n        # reverse=True ensures descending order\n        sorted_chars = sorted(count.keys(), key=lambda x: count[x], reverse=True)\n        \n        # Step 3: Build the result string\n        res = []\n        for char in sorted_chars:\n            # Append the character 'count[char]' times\n            res.append(char * count[char])\n            \n        return \"\".join(res)"
  },
  "Custom Sort String": {
    description: "You are given two strings order and s. All the characters of order are unique and were sorted in some custom order previously. Permute the characters of s so that they match the order that order was sorted.",
    explanation: "Count the occurrences of each character in s. Iterate through order and append the characters to the result string based on their count in s. Finally, append any remaining characters from s that were not in order.",
    implementation: "class Solution:\n    def customSortString(self, order: str, s: str) -> str:\n        # Step 1: Count the frequency of characters in string 's'\n        from collections import Counter\n        count = Counter(s)\n        \n        res = []\n        \n        # Step 2: Iterate through 'order' to maintain the custom sorting order\n        for char in order:\n            if char in count:\n                # Append the character as many times as it appears in 's'\n                res.append(char * count[char])\n                # Remove the character from the count dictionary\n                del count[char]\n                \n        # Step 3: Append any remaining characters that were in 's' but not in 'order'\n        # Their relative order doesn't matter\n        for char, freq in count.items():\n            res.append(char * freq)\n            \n        return \"\".join(res)"
  },
  "Relative Sort Array": {
    description: "Given two arrays arr1 and arr2, the elements of arr2 are distinct, and all elements in arr2 are also in arr1. Sort the elements of arr1 such that the relative ordering of items in arr1 are the same as in arr2. Elements that do not appear in arr2 should be placed at the end of arr1 in ascending order.",
    explanation: "Count the frequencies of elements in arr1. Iterate through arr2 and add elements to the result based on their frequency. Then, sort the remaining elements and append them.",
    implementation: "class Solution:\n    def relativeSortArray(self, arr1: List[int], arr2: List[int]) -> List[int]:\n        # Step 1: Count the frequency of each element in arr1\n        from collections import Counter\n        count = Counter(arr1)\n        \n        res = []\n        \n        # Step 2: Add elements to the result according to the order in arr2\n        for num in arr2:\n            if num in count:\n                # Extend the result list with the element repeated 'count[num]' times\n                res.extend([num] * count[num])\n                # Remove the processed element from the counter\n                del count[num]\n                \n        # Step 3: Handle the remaining elements that were not in arr2\n        # They need to be sorted in ascending order\n        remaining = []\n        for num, freq in count.items():\n            remaining.extend([num] * freq)\n            \n        # Sort the remaining elements and append to the result\n        remaining.sort()\n        res.extend(remaining)\n        \n        return res"
  },
  "Pancake Sorting": {
    description: "Given an array of integers arr, sort the array by performing a series of pancake flips. In one pancake flip we do i = k and reverse the sub-array arr[0...k]. Return an array of the k-values corresponding to a sequence of pancake flips that sort arr.",
    explanation: "Find the largest element, flip it to the front, then flip it to its correct position at the end. Repeat for the remaining unsorted portion.",
    implementation: "class Solution:\n    def pancakeSort(self, arr: List[int]) -> List[int]:\n        res = []\n        \n        # We want to place the largest element at the end, then the second largest, etc.\n        # 'n' represents the size of the unsorted portion of the array\n        for n in range(len(arr), 1, -1):\n            # Find the index of the maximum element in the unsorted portion\n            max_idx = arr.index(max(arr[:n]))\n            \n            # If the maximum element is already at its correct position, do nothing\n            if max_idx == n - 1:\n                continue\n                \n            # If the maximum element is not at the front, flip it to the front\n            if max_idx != 0:\n                # Flip the sub-array from 0 to max_idx\n                arr[:max_idx+1] = reversed(arr[:max_idx+1])\n                res.append(max_idx + 1) # 1-based index for the flip\n                \n            # Now the maximum element is at the front. Flip it to its correct position 'n-1'\n            arr[:n] = reversed(arr[:n])\n            res.append(n) # 1-based index for the flip\n            \n        return res"
  },
  "Wiggle Sort II": {
    description: "Given an integer array nums, reorder it such that nums[0] < nums[1] > nums[2] < nums[3]....",
    explanation: "Sort the array, then place the smaller half in the even indices (from end to start) and the larger half in the odd indices (from end to start) to avoid placing equal medians adjacent to each other.",
    implementation: "class Solution:\n    def wiggleSort(self, nums: List[int]) -> None:\n        \"\"\"\n        Do not return anything, modify nums in-place instead.\n        \"\"\"\n        # Step 1: Sort the array to easily separate smaller and larger elements\n        nums.sort()\n        \n        # Step 2: Find the middle index to split the array into two halves\n        # We use (len(nums) - 1) // 2 so that the left half is larger if the length is odd\n        mid = (len(nums) - 1) // 2\n        \n        # Step 3: Extract the smaller and larger halves\n        # Reversing them ensures that if there are duplicate medians, \n        # they are placed as far apart as possible\n        left_half = nums[:mid+1][::-1]\n        right_half = nums[mid+1:][::-1]\n        \n        # Step 4: Interleave the elements into the original array\n        # Even indices get elements from the left half (smaller elements)\n        nums[::2] = left_half\n        # Odd indices get elements from the right half (larger elements)\n        nums[1::2] = right_half"
  },
  "Maximum Gap": {
    description: "Given an integer array nums, return the maximum difference between two successive elements in its sorted form. If the array contains less than two elements, return 0. You must write an algorithm that runs in linear time and uses linear extra space.",
    explanation: "Use Radix Sort or Bucket Sort to achieve O(N) time. Bucket sort involves calculating the minimum possible maximum gap, creating buckets of that size, placing elements in buckets, and then comparing the maximum of one bucket with the minimum of the next non-empty bucket.",
    implementation: "class Solution:\n    def maximumGap(self, nums: List[int]) -> int:\n        # Base case: if less than 2 elements, the gap is 0\n        if len(nums) < 2:\n            return 0\n            \n        min_val, max_val = min(nums), max(nums)\n        \n        # If all elements are the same, the gap is 0\n        if min_val == max_val:\n            return 0\n            \n        n = len(nums)\n        # Calculate the minimum possible maximum gap (pigeonhole principle)\n        # The maximum gap must be at least ceil((max_val - min_val) / (n - 1))\n        import math\n        bucket_size = math.ceil((max_val - min_val) / (n - 1))\n        \n        # Create buckets to store the min and max values in each range\n        # We need (n - 1) buckets\n        buckets = [[float('inf'), float('-inf')] for _ in range(n - 1)]\n        \n        # Place each number into its corresponding bucket\n        for num in nums:\n            if num == min_val or num == max_val:\n                continue\n            # Determine the bucket index\n            idx = (num - min_val) // bucket_size\n            # Update the min and max for the bucket\n            buckets[idx][0] = min(buckets[idx][0], num)\n            buckets[idx][1] = max(buckets[idx][1], num)\n            \n        # Calculate the maximum gap by comparing adjacent non-empty buckets\n        max_gap = 0\n        prev_max = min_val\n        \n        for i in range(n - 1):\n            # Skip empty buckets\n            if buckets[i][0] == float('inf'):\n                continue\n            # The gap is the difference between the current bucket's min and the previous bucket's max\n            max_gap = max(max_gap, buckets[i][0] - prev_max)\n            prev_max = buckets[i][1]\n            \n        # Finally, compare with the absolute maximum value\n        max_gap = max(max_gap, max_val - prev_max)\n        \n        return max_gap"
  }
};
